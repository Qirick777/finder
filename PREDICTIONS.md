# 봉건 밭 경제 — 장기 예측안 (로그 대조 기준서)

> **v1.1** — R1(지대 재투자)·R2(출근 관성)·R3(개체별 개간 상한)·R5(경쟁 대칭)·R6(유령 용량)
> 반영판. R4(지대 비중 ≈20%)는 의도 사항으로 확정. 궤적·판정선은 전부 수정된 빌드 기준.

> **용도**: 설계가 "올바른가"(계층이 실제로 창발하는가) vs "기능만 대략 있는가"를 가르는 기준.
> 아래 예측은 **코드의 실제 상수·산식에서 유도**했고(추정치는 `≈` 표기), 실행 로그와 대조해
> 유사/부분 일치/전혀 없음을 판정한다. 기존 테스트 3층(evotest·checkall·게이트 명령)은 전부
> "기능이 발동한다"까지만 보증한다 — **동역학(며칠 뒤 무엇이 되는가)은 이 문서가 첫 검증층**이다.

## 0. 대조 절차 (이것부터)

1. 게임 시작 직후 **`/evolog on`** (기본 꺼짐 — 켜지 않으면 대조 불가). 켜져 있으면 매일 기상
   직후 인구조사에 **`밭` 1줄**(구획·무주·총 타일·최대 구획·지주·상시소작·계정합)이 자동 기록되어
   §4 스냅샷 표와 day 단위로 그대로 대조된다.
2. day 10 / 20 / 30 / 50 에 **`/evosim lords`** 실행 결과를 캡처(지주별 상세 — 자동 1줄의 보완).
3. `<서버폴더>/evosim-events.log` 를 통째로 제출. 모든 줄에 `[dN 시간대]` 접두가 있어
   아래 "예상 day"와 직접 대조된다. 대상 이벤트 태그: `밭개간` `밭확장` `상시소작` `소작수확`
   `소작해제` `밭선점` `구제`.
4. 지주로 관측된 개체는 **검사봉**으로 특성 확인 — §2의 "지주는 동기 특성 보유자여야 한다"가
   이 설계의 핵심 예측이므로, 무특성 지주 1명만 나와도 만족 배선 결함이다.

## 1. 모델 입력 (코드 실측값)

| 항목 | 값 | 출처 |
|---|---|---|
| 하루 | 24000틱, 밭 타일은 하루 1회 익음 | `FarmEconomy.RIPEN_TICKS` |
| 채집 수입 | 남 ≈9/일, 여 ≈3/일 (2.0×성별 1.5/0.5×~3트립) | `FoodEconomy.BASE_YIELD` 주석 |
| 밭 수확 단가 | 남 0.75/타일, 여 0.25/타일 (0.5×성별×gather) | `MimicFarmGoal` yield |
| 일일 수확 용량 | 12타일 (부지런 14 / 게으름 9 / 노년 ×0.5) | `FarmEconomy.capacity` |
| 성년 하루 소모 | 3.12 (이동 기준, 만족 산식 입력) · 실효 ≈2~2.5 | `consumptionPerDay` 실측 |
| 만족 기준(무특성) | 3.12×2일×σ2 = **12.5** (재개 10.0) | `Satisfaction` |
| 만족 기준(안분지족·무욕) | 6.2 / (미래지향) 18.7 | σ1 / comfortDays 3 |
| 개간 문턱 | 30+예비 6 = **36** (2호 51, 3호 73.5) | `newFarmCost`+`INVEST_RESERVE` |
| 고용 게시 | 부족분 = T−가구용량 ≥ **10** → T≥22부터 | `shortfall`, `MIN_JOB` |
| 상시 승격 | 같은 밭 **3일 연속** 배정 | `PROMOTE_DAYS` |
| 지대 | 소작 수확의 30% → 밭 계정 → 밤 정수 이체 | `FEE` |
| 확장 | 타일당 3, **개체당** 3타일/일(R3) · 자영은 저장고+예비 6 | `EXPAND_*` |
| **지대 재투자(R1)** | 소작 구획 확장 자금 = 밭 계정(주인 불만족일 때만) — 소작 1인 ≈0.9타일/일, 2인 ≈1.4~2타일/일 | `reinvestTiles` |
| **케어 예산(v1.2)** | 가구 노동(주인+동거 배우자, 부부 24)을 가까운 밭부터 소진 — **케어가 닿지 않는 원거리 밭은 100% 소작** | `allocateCare` |
| 능력 게이트 | 무능력 주인 구획 상한 35타일 | `SKILL_GATE_TILES` |

**모집단 실측** (1세대 생성기 `Genetics.randomFirstGen` 10,000명 몬테카를로, 발현 기준):

| 집단 | 비율 | 의미 |
|---|---|---|
| 욕심 4.2% · 부지런 3.9% · 야망가 4.1% · 경쟁 3.9% | 개별 | 만족 정지를 무력화하는 동기 특성 |
| **개간 동기 보유(넷 중 하나, 무욕 제외)** | **15.1%** | 지주가 "될 수 있는" 전부 |
| 대규모 능력(약초학·채집꾼·손재주·요리사) | 15.8% | 구획 35 초과 자격 |
| **동기+능력 동시(대지주 잠재)** | **1.6%** (남성 0.7%p) | 20명 마을 기대 0.3명 |
| 무욕(영구 비개간·비확장) | 3.9% | 계층 하방 고정층 |

## 2. 구조 예측 — 왜 평민은 절대 지주가 못 되는가 (수치 증명)

개간은 새벽 만족 판정을 통과한(=불만족) 개체만 실행한다(`growFarms` skip). 무특성 성년의
만족 기준은 12.5인데 개간 문턱은 36 — **기준의 2.9배**. 잉여가 12.5를 넘는 순간 만족으로
전환되어 개간이 영구 차단된다. 미래지향(18.7)도 못 넘는다. 따라서:

> **P0 (전제 예측)**: 로그의 `밭개간` 주체는 전원 욕심·부지런·야망가·경쟁(열세 상태) 중
> 하나를 발현한 개체다. 예외 0건. — 위반 시 `updateMotivation`/`Satisfaction.satisfied` 결함.

같은 이유로 지주 비율의 상한은 동기 발현율 **≈15%**다. 지주가 그보다 흔하면 만족 배선이
죽은 것이고, 0이면 축적 경로가 막힌 것이다(§5 판정선).

## 3. 아키타입별 궤적 (day = 월드 시작일 기준, 성체 스폰 가정)

### A. 표준 지주 — 동기 특성 남성 (인구의 ≈7%)
독신·소가족 가정. 잉여 ≈+6.5/일.

| 이정표 | 예상 day | 대조 로그 |
|---|---|---|
| 밭개간 #1 (36 축적) | **5~8** | `밭개간 신규 구획 N(1번째)` |
| 확장 진행 (자금 게이트로 +2~3/일) | 6~13 | `밭확장 자영 +3타일` 연일 |
| T≥22 → 첫 고용 게시·배정 | **12~15** | 첫 `소작수확` |
| 상시 소작 승격 — **첫 고용 +3일, 결정론(R2)** | **15~18** | `상시소작 3일 연속 출근` |
| 확장권 이전 → 지대 재투자 성장(R1) | 승격 즉시 | `밭확장 재투자` 로그로 전환(자영→재투자) |
| 재투자 성장률 | 소작 1인 ≈0.9타일/일 → 2인부터 ≈1.4~2 | `밭확장 재투자` 빈도 |
| 무능력 주인 구획 — **정확히 35에서 정지** | 승격 +4~7일 | lords — 35 정지 자체가 SKILL_GATE 작동 증거 |
| 능력 주인 구획 — T5(49) 도달 | **25~35** | lords 최대 구획 49 |
| 밭개간 #2 (51 축적, 소작 보유 조건) | **18~23** | `밭개간 (2번째) — 비용 45` |
| day 30 상태 | 구획 2~3 · 총 55~90타일 · 상시 2~4 | lords·census `밭` 줄 |
| day 50 상태 (욕심·부지런 — 무한 동기) | 구획 3~5 · 총 110~170타일 | lords·census |

- 대가족 가장이면 잉여 +2~4/일 → 개간 #1이 day 10~18로 지연.
- **부부 지주(v1.2)**: 아내도 밭을 수확(가족 노동 24용량) → 첫 고용 문턱이 T≥34(독신 22)로
  올라가고 자영 소득은 2배 — 고용 시점은 2~4일 늦지만 축적·확장은 더 빠름. 다구획이 되면
  원거리 밭은 케어 예산이 닿지 않아 **자동으로 100% 소작 운영**(F8로 회귀 감시).
- 여성 동기 개체는 자기 채집(3/일)만으로는 수십 일이 걸리므로, **부유 가구 소속일 때만**
  (가구 저장고 공유) 지주가 된다. 여성 단독 가구 지주 = 사실상 0 예측.

### B. 동기별 분기
| 특성 | 종착 예측 | 대조 |
|---|---|---|
| 욕심·부지런 | 영구 불만족 — 개간 무한 반복(비용 ×1.5 체증으로 감속) | day 50에 구획 3~4 |
| 야망가 | 총 49타일 도달(**day 20~28**) 후 만족 → **노동 은퇴**. 은퇴하면 본인 용량이 슬롯 산식에서 빠져(R6) 밭 전체가 소작 커버로 전환 — 완전한 지대 생활 계층 | 49~55에서 정체 + `밭수확` 소멸 + `소작수확`·`상시소작` 증가 |
| 경쟁 | 반경 48블록에 더 부유한 이웃이 있을 때만 동기 → **지주 곁에서만 2호 지주 출현** (밭의 공간 군집) | `밭개간` 좌표가 기존 밭 근처에 몰림 |
| 무욕 | 개간·확장 0 (부유해도) | 해당 개체 `밭개간` 0건 |
| 평민(≈78%) | 지주 0 · 소작 후보 · 자급 | — |

### C. 소작층 — 누가 고용되고 누가 승격되는가
고용 후보는 "저장고가 넉넉하지 않은" 개체뿐(`larderComfortable` 제외 + 소유주·동거 가구 제외).
소작 일당: 남 ≈6.3(0.7×0.75×12), 여 ≈2.1.

> **P1 (v1.1 — 출근 관성 반영)**: 어제 출근자는 넉넉 필터를 면제받으므로(R2), **부족분이
> 유지되는 한 첫 고용 +3일에 승격이 결정론적으로 일어난다**. 승격 실패는 이제 정상 변동이
> 아니라 결함이다(F3). 신규 채용의 성비는 빈곤 후보 풀을 따르므로 여성이 다소 우세할 수
> 있으나(채집 3/일), v1.0이 예측했던 "승격의 여초 편중"은 폐기 — 남성도 관성으로 완주한다.

## 4. 마을 수준 스냅샷 예측 (성체 20명 기준 — 인구 비례 스케일)

| 시점 | 구획 수 | 지주 수 | 최대 단일 구획 | 상시 소작 | 비고 |
|---|---|---|---|---|---|
| day 10 | 1~3 | 1~3 | 9~18 | 0 | 자영 확장기 |
| day 20 | 2~5 | 1~3 | 25~35 | 1~3 | 첫 고용·승격·재투자 개시 |
| day 30 | 3~6 | 2~4 | **35(무능력 정지) 또는 40~49(능력)** | 2~5 | census `밭` 줄과 직접 대조 |
| day 50 | 5~10 | 2~4 | 35 / 49 이중 봉우리 | 3~7 | 구획 크기 분포가 게이트 경계에 몰림 |

계층 구성(day 30+): 지주 10~15% / 상시 소작 10~20% / 자급 평민 65~80%.
- **구획 크기 분포의 이중 봉우리(35·49)가 v1.1의 결정적 예측** — 능력 게이트와 재투자가 둘 다
  작동해야만 나오는 모양이다. 봉우리가 27~31에 있으면 R1 수정이 실패한 것(F6).
- 무주지(`밭선점`)·상속(`farminherit` 경로)은 사망 발생 이후에만 — day 30 이전 0~2건.

## 5. 판정 기준 — "유사" vs "전혀 없음"

**유사(설계 성립)**: P0 예외 0건 + 이정표 4개 이상이 예상 day ±50% 이내 + day 30 스냅샷이
표의 구간 내.

**설계 실패 신호(F코드 — 발견 즉시 해당 함수 역추적)**:

| 코드 | 관측 | 1순위 의심 |
|---|---|---|
| F1 | 동기 남성이 있는데 day 12까지 `밭개간` 0 | 축적(`settleHome` 입금) 또는 `findFarmSite` 전멸 |
| F2 | 개간 후 10일 내 `소작수확` 0 | `assignDawn` 후보 필터 과도 / `shortfall` 미달 |
| F3 | 첫 고용(`소작수확` 개시) +5일 내 `상시소작` 0 | 출근 관성(R2 수정 — LAST_ASSIGNED 면제) 회귀 |
| F4 | 전 구획 22 미만 정체 + `밭확장` 소멸 | 자금(`INVEST_RESERVE`) 또는 만족 조기 발동 |
| F5 | 지주 비율 > 25% 또는 무특성 지주 존재 | `Satisfaction`/`updateMotivation` 미작동 |
| F6 | 승격 후 `밭확장 재투자` 0 — 구획이 27~31 동결 | 지대 재투자(R1 수정 — 계정 자금·호출 순서) 회귀 |
| F7 | 은퇴 지주(만족) 밭의 익은 타일이 만성 잔존 | 유령 용량(R6 수정 — ΣC 만족 제외) 회귀 |
| F8 | 다구획 지주의 원거리 밭에서 익은 타일 만성 잔존(배정 부족) | 케어 예산(R7 수정 — `allocateCare` 중복 차감) 회귀 |

## 6. 설계 리스크 처리 현황 (v1.1 — R1·R2·R3·R5·R6 수정 반영, R4 의도 확정)

v1.0 모델링에서 발견한 6개 리스크의 처리. 수정분은 §5의 F코드(F3·F6·F7)로 회귀를 감시한다.

- **R1 소작 확장 동결 → 해소(지대 재투자)**: 자금 원천을 소작농 저장고에서 **밭 계정**으로
  변경(`reinvestTiles`, growFarms→settleRent 순서). 재투자 여부는 주인의 불만족이 결정 —
  만족·무욕 주인은 계정 전액 착복. 노동 주체(소작농)·주인 확장 금지는 확정 사양대로 유지.
  → T4·T5·능력 게이트가 산 규칙이 됨. 회귀 감시 F6, 게이트 `farmgrow`(farm_grow_reinvest).
- **R2 승격 진동 → 해소(출근 관성)**: 어제 같은 구획 출근자는 넉넉 필터 면제(예약석 원리의
  수습기 소급). 신규 구직만 빈곤 조건. → 승격이 결정론화. 회귀 감시 F3.
- **R3 확장 상한 → 해소(개체 단위)**: growFarms 에 개체별 당일 카운터 — 다구획 주인 1인의
  개간 합계가 3타일/일을 넘지 않음.
- **R4 지대 비중 ≈20% → 의도 사항으로 확정**(사용자 결정): 일반 지주의 상은 "부농+품일꾼"이
  맞다. 순수 지대 생활은 야망가 은퇴(§3-B)와 만족 상속인의 T≥22 밭(R6 효과)으로만 나타난다.
- **R5 경쟁 비대칭 → 해소**: 이웃 부 정의를 자기 부와 동일(저장고+소유 밭 계정 합)로 대칭화.
- **R6 유령 용량 → 해소**: `assignDawn` 가구 ΣC 에서 만족 구성원 제외. 은퇴·상속 지주의
  T≥22 밭이 전량 소작 커버로 자동 전환(임대 순환 완성). 회귀 감시 F7.
  - 의도적 잔존: T<22 상속 밭은 부족분 미달(최소 일감 10)로 방치되다가 가문이 가난해지면
    (만족 해제) 자영 재개 — "밭=보험" 완충으로 남겨둔다(수정 아님, 관찰 대상).
- **R7 다구획 중복 차감 + 가족 노동 비대칭 → 해소(v1.2, 케어 예산)**: 종전엔 ①주인 용량 12가
  소유 전 구획에서 각각 차감돼 원거리 밭이 구획당 12타일씩 과소 고용(썩는 밭), ②가구 ΣC 는
  동거 성년 전원을 세는데 실제 수확은 주인만 가능(부부 밭의 유령 12). 수정: 가구 노동
  (주인+동거 배우자 — MimicFarmGoal 의 배우자 수확 허용과 대칭)을 **가까운 구획부터 소진**하는
  예산으로 — 케어가 닿지 않는 밭은 부족분 전량 게시 = **100% 소작 운영**(사용자 확정 사양).
  가족 수확분은 지대 없는 100%(소작 회계와 분리). 회귀 감시 F8, 게이트 farmfamily·farmcare.
  참고: '완전 관리 은퇴(영주화)' 설계는 검토 후 사용자 결정으로 **폐기** — 지주는 규모와 무관하게
  자기 손이 닿는 밭은 계속 자영한다(부농 상 유지).

## 7. 테스트 영역 4층 지도 — 각 층이 보증하는 것 / 못 하는 것

| 층 | 무엇 | 보증하는 것 | 보증 못 하는 것 |
|---|---|---|---|
| 1 | `/evotest all` 150케이스 (헤드리스) | 순수 산식의 명세 일치 — 배치 수열, 회계 항등식(소작몫+지대=수확), 용량·부족분·비용 산식, 만족 진리표, 유전·발현 | 마인크래프트 배선 전부(goal·SavedData·틱), 시간 흐름 |
| 2 | `/evosim checkall` 37단계 (인게임 관문) | "기능이 실제 게임에서 발동한다" — 직전 상태 조성 → 결과값 변화 판정. 단일 개체·단일 기구·분 단위 | 기구 간 상호작용, 하루 단위 이상의 동역학 |
| 3 | farm 게이트 16종 + 진단 지도 (TESTING.md) | 실패 슬러그 → 의심 함수 역추적(수리 도구) | 신규 결함 발견(재현 전용) |
| 4 | **본 문서 + evosim-events.log** (장기 대조) | **설계의 타당성** — 계층이 예측 궤적대로 창발하는가, 죽은 규칙은 없는가 | (실행 전까지 무검증 — 현재 유일한 공백) |

1~3층이 전부 통과해도 4층은 실패할 수 있다(예: R1이 사실이면 모든 기능이 "작동"하지만
대형 구획·능력 게이트는 영원히 안 쓰인다). 역도 성립하지 않는다 — 4층 대조는 1~3층 통과를
전제로만 의미가 있다(기능 결함과 설계 결함의 분리).

## 8. 소작농화 로그 판독 프로토콜 (로그 수령 시 이 절차대로 점검 — 질문 없이)

**전제**: 오버월드 평지(잔디·나무 존재), `/evolog on` 상태로 관측 시작, `evosim-events.log` 통째 제출.
모든 줄은 `[dN 시간대] 유형 #id(성별단계 H) 내용 @x,z` — id 로 개체 추적, dN 으로 시점 대조.

### 8-1. 소작농화 사슬 (이 순서의 로그가 사슬로 이어져야 한다)

```
등장[특성]  →  배정(연속 1) → 배정(연속 2) → 상시소작(3일)  →  소작수확 ×N/일
   │                                                        ↘  지대(밤, 주인에게)
   │                                                        ↘  밭확장 재투자(주인 분발 시)
   ├─ (지주 경로) 분발 → 밭개간 → 밭확장 자영 → …→ 만족(은퇴) → 지대만 수취
   └─ (몰락 경로) 소작해제(통근/구제 불이행) · 구제(위급) · 사망 → 밭상속/밭선점
```

### 8-2. 불변식 — 위반 1건이라도 발견 즉시 결함 (기록 자체의 무결성 포함)

| # | 반드시 성립 | 위반 시 1순위 의심 |
|---|---|---|
| I1 | `밭개간` 주체 id 의 `등장` 특성[]에 욕심·부지런·야망가·경쟁 중 하나 존재(무욕 없음) | P0 — `Satisfaction`/`updateMotivation` |
| I2 | `상시소작` 이전에 같은 id·같은 구획의 `배정 연속 1→2` 가 선행 | 승격 경로 우회(setTenant 직접 호출 잔재) |
| I3 | 같은 구획의 `소작수확` 지대 적립 합 ≈ `지대` 이체 합 + 마지막 이월(±1 유닛) | 회계 누수 — `settleRent`/`ownerShare` |
| I4 | `지대`는 `소작수확`이 있던 날에만(무수확 무지대 — 선적립 검증 무대 제외) | 유령 적립 |
| I5 | `밭확장 재투자`는 그 구획에 상시 소작 존재 ∧ 주인이 분발(마지막 전환이 `분발`) 상태에서만 | R1 게이트 회귀 |
| I6 | 모든 활동 로그의 id는 선행 `등장`이 있고, `사망`/`아사`/`자연사` 이후 그 id 활동 없음 | 기록 누락/유령 개체 — introTick/사망 훅 |
| I7 | census `인구` 총원 증감 = `등장`(무대 제외) − 사망류 누적과 일치 | 로그 누락(어느 죽음 경로가 무기록인지 역산) |
| I8 | `배정` 없는 날의 연속일은 리셋 — `배정 연속 k` 다음에 공백일이 있으면 다음은 `연속 1` | LAST_ASSIGNED 롤오버 |

### 8-3. 시점 기준 (평지 초원 · 성체 10~20 스폰 — §3·§4의 요약 판)

| day | 기대 로그 | 없으면 |
|---|---|---|
| 0 | 전 개체 `등장`(특성 대조표 — 검사봉 불필요) + 매일 `인구`/`밭` census | /evolog 미점등 |
| 5~8 | 첫 `밭개간` — **단, 등장 특성에 동기 남성이 0이면 밭 0이 정상**(발현율 15%, 결함 아님) | F1(동기 개체 존재 시만) |
| 12~15 | 첫 `배정` | F2 |
| 15~18 | 첫 `상시소작` (첫 배정 +3일 정확히 — R2 결정론) | F3 |
| 승격±1 | `밭확장`이 `자영`→`재투자`로 전환 | F6 |
| 18~23 | `밭개간 (2번째)` | 자금 축적 확인(지대·수확 로그로 역산) |
| 20~28 | 야망가 지주의 `만족` + 이후 `밭수확` 소멸·`지대` 지속 | R6/야망 축 |
| 30 | census `밭` 줄이 §4 구간 내 | 스냅샷 전체 대조 |

### 8-4. 판독 순서 (로그를 받으면 이 5단계 — "왜 주셨지?" 금지)

1. **census 최종 줄** → §4 표와 스냅샷 대조(구획·타일·최대·지주·상시소작).
2. **`등장` 전수** → 특성 분포 확인(동기·능력 개체 수 = 기대 지주 상한) — [무대] 표식은 제외.
3. **불변식 I1~I8** grep 검사(위 표 순서 — 하나라도 깨지면 그 지점이 최우선 결함).
4. **이정표 시점**(8-3) 대조 — ±50% 밖이면 §5 F코드로 분기.
5. 어긋난 항목 → §5 F코드·TESTING.md 진단 지도의 의심 함수로 역추적 보고.

### 8-5. 기록 원칙 (로그 배출부 감사 결과 — 전 지점 "결과 후 기록" 확인)

밭 사슬 로그는 전부 **상태 변경이 실제로 끝난 뒤** 배출된다(배정=ASSIGNED 확정 후, 지대=이체 후,
확장=설치·차감 후, 승격=setTenant 후, 만족/분발=캐시 전환 후 — 시도·호출 시점 기록 없음).
죽음 4경로(아사 성체/유아·자연사·피해 사망)와 탄생 2경로(등장·출산) 모두 기록되며, 무대 개체는
`등장`/`사망`에 `[무대]` 표식이 붙어 필터 가능. 관측 중 검증 명령을 섞으면 무대 표식으로 걸러 읽는다.

---
*모집단 수치는 `Genetics.randomFirstGen` 10k 표본(시드 42) 실측. 세대가 지나면 유전·선택으로
비율이 이동하므로 day 50 이후 대조에는 `/evolog census`의 세대 통계를 함께 볼 것.*

# 역참(驛站) 설계서 — 합의 사항 기록

작성 시점: 지식인 층(대학·병원) 검증 완료 뒤, 목축 시스템에 앞서 잊지 않게 적어 둔다.

## 구현 기록 (사용자 승인 뒤 코딩)
- 역참은 사용자가 만든 **이정표**(home files/signpost.nbt, 3×6×3: 석재 벽돌 밑동·돌담·울타리 기둥·동서 팔·매단 표지판 둘·랜턴)로 한다.
- `SignpostStore`(등기: 밑동·회전·옆 길 칸) · `SignpostPlanner`(도면 회전·표지판 글씨·자리 고르기) · `RelayNet`(경로표) · MimicEntity(시공·착공 판정·길찾기 경유) · FarmTicker/MimicVisitGoal(자격 OR).
- 표지판에는 팔 방향으로 다음 마디가 놓인 시설 **종류 이름**을 가까운 순으로 최대 4줄 쓰고, 화살표는 보는 면마다 뒤집는다. 밤 정산 뒤 전부 다시 쓴다.
- 마디 = 이정표의 길 칸 + 문 있는 시설(상한 0 인 종류는 제외)의 바깥 문 칸. 마디 사이 연결은 직선 100 안. 표는 메모리에만 두고 등기·밤·월드 교체 때 다시 만든다.
- 길찾기는 표적이 직선 120 밖일 때만 망을 쓴다: 내 마디(96 안)에 먼저, 닿으면 다음 마디. 구간은 닿을 때까지(1.5블록) 유지. 로그 `이정표경유`.
- 착공 주체는 가구 정산의 군주(추종자 최다 식구, 추종자 ≥ 6). 방아쇠는 §1 그대로(어느 마디에서도 96 넘게 떨어진 추종 가구). 값 8. 자리 점수 = 새로 덮는 미도달 가구 수 → 미도달에 가까운 곳 → 96 에 가까운 곳. 팔은 길과 나란히(교차로는 좌표 홀짝).
- 이정표의 랜턴은 지면+5 라 가로등 어둠 지도에서 등으로 친다(그 자리엔 가로등이 따로 서지 않는다). 기둥 열은 길 장애물.
- 명령: `evosim signposts`(보고·표), `evosim signauto`(무대: 즉시 세움), `evosim relaytest x z hx hz`(무대: 망 이동), `evosim signtest x z rot`(무대: 도면 회전·글씨).


## 0. 왜 필요한가 (실측)
- 길찾기 사거리(FOLLOW_RANGE 160)와 큰 건물 우회 탐색 예산 때문에 한 번에 낼 수 있는 경로가 사실상 150블록이다.
  기숙 상한을 150 으로 낮춘 이유이며(165블록 학생이 4800틱 제자리), 영지가 300블록 넘게 자라면(런 35) 대학·병원 자격이
  "집에서 직선거리 N 이내" 규칙에 묶여 먼 가구는 영영 못 온다.
- 입학 깔때기 계측(런 34 d23 · 런 35 d30~31): 반경 150 밖으로 걸러진 상급 성년은 하루 1~5명. 지금 마을에서는 주된 병목이
  아니지만(상급 성년 절대 수와 상급 소년 0명이 병목), 영지가 자랄수록 효과가 커지고 시장·교역의 발이 된다.

## 1. 역참
- 작은 도면 하나(이정표·등불·종이 달린 기둥 정도), 건축비는 우물보다 싸게. FacilityStore 에 우물·가로등처럼 등기(영구 저장은 이것뿐).
- 군주가 길(RoadPlanner) 위에 간격 96 으로 세운다. 시작 규칙: "시설에서 96 넘게 떨어진 추종 가구가 생기면 그 길 위에 하나".
- 모든 시설의 바깥 문 칸(FacilityTemplate.entryDoors)은 자동으로 역참 노릇을 한다(망의 끝점).
- 인지반경 96. 역참 간격 96 이면 한 구간이 항상 사거리 160 안이다.

## 2. 경로표 (거리 벡터, 밤 정산에 한 번)
- 역참마다 시설별 한 줄: `시설: 거리 0`(내 인지반경 안) / `거리 1, 다음 A` / `거리 2, 다음 B` …
- 같은 시설로 가는 이웃이 둘이면 거리가 짧은 쪽 하나만, 거리가 같으면 등기 번호가 작은 쪽으로 고정(밤마다 표가 흔들리지 않게).
- 시설 종류별 거리 상한을 넘는 줄은 저장하지 않는다(그 역참은 그 시설을 모른다). 상한(Facilities 상수):
  학교 1 · 교회 2 · 병원 2 · 대학 3 · 막사·우물·풍차·구빈원 0(역참을 쓰지 않음).
- 역참→역참 표도 같이 만든다(귀가용).
- 표는 파생 자료라 NBT 에 쓰지 않는다. 메모리에만 두고 월드 로드 뒤 첫 조회·밤 정산·역참/시설 등기·말소 때 다시 만든다(더러움 표시).
  계산량은 S×F, S×S 너비우선탐색(S=역참 수, F=시설 수) — 하루 한 번, 틱마다 하는 것은 없다.
- `evosim deed` 의 역참 문서에 표를 그대로 찍는다.

## 3. 미믹의 이동 (개체별 저장 없음)
- 목표 시설이 직접 경로 범위(120) 밖이면 "그 시설 줄이 있는 가장 가까운 역참"을 다음 목표로 잡고, 도착하면 그 역참의 표에서
  다음 역참(거리가 반드시 1 작음)으로 간다. 거리 2→1→0 단조 감소라 왕복 진동이 없다. 한 구간은 닿을 때까지 유지(문 경유 이력과 같은 규칙).
- 치환 자리는 지금 문 칸 경유를 넣은 길찾기 경유점(MimicEntity.createNavigation → facilityGate) 한 곳. 리시·귀가·등교 goal 은 손대지 않는다.
- 귀가: 집 좌표로 "집에서 가장 가까운 역참"을 찾고(역참 수만큼 비교, 밤마다 집별 캐시 가능), 지금 선 역참에서 그 역참까지
  역참→역참 표대로 간 뒤 마지막 구간은 직행. 역참으로 나간 사람은 첫 구간이 집에서 사거리 안 역참이었으니 그 역참이 반드시 있다.
- 미믹이 갖는 것은 "지금 향하는 다음 역참" 메모리 필드 하나뿐(저장 안 함, 재시작하면 선 자리에서 다시 묻는다 — 표가 결정적이라 답이 같다).
- 예외: 역참이 낮에 헐려 다음 구간이 사거리 밖이면 남은 역참 중 목표에 가장 가까운 것까지 가서 직행 시도. 실패하면 리시 정체 진단이 남고 밤에 표가 새로 잡힌다.

## 4. 자격 판정 교체
- 입학·입원·통학 자격을 "직선거리 N 이내"에서 "집에서 가장 가까운 역참에 그 시설 줄이 있는가(종류별 상한)"로 바꾼다.
  학교 1 이면 두 번째 역참까지, 대학 3 이면 네 번째 역참까지(약 400블록) 닿는 가구가 자격을 얻는다.
- 마지막 구간은 기존 규칙(통학 64·기숙·진료권 64)이 그대로 맡는다. 기숙 150 규칙은 망이 없을 때의 예비로 남긴다.

## 5. 코딩 순서와 검증
1. 역참 도면·등기(FacilityStore 갈래 RELAY).  2. 밤 정산 경로표 + 역참 문서 표 출력.  3. 길찾기 경유 치환 + 자격 판정 교체.
4. 런 35 월드(scratchpad/world-r35b)에서 150 밖 학생이 실제로 역참을 거쳐 입학·착석하는지 측정(evosim exitx 방식의 진단 병행).

## 6. 이후 연결
- 시장·교역: 상인이 역참 망을 따라 이웃 장터로 오간다(값 차이가 동기). 자원 후보는 장작(연료)·석재·가축(목축) 순으로 논의됨.

# 농노·자유소작·유생 — 시스템 명세 (코딩 전)

신분을 **저장하지 않는다**. 매일 밤 파생값으로 계산한다(`SocialRank` 가 이미 그 방식이다).
새 저장 축을 만들지 않고, 이미 있는 신세 원장·예속 일수·누진 지대에 인자만 더한다.

## 1. 이미 있는 것 (그대로 쓴다)
- `AllegianceStore.Bond{forgiven, owed}` — 신세 간선. `forgiven` 은 은혜(무상), `owed` 가 빚.
- `AllegianceStore.owedOf(id)` — 총 빚. `repay(id, amount)` — 상환.
- `AllegianceStore.noteBondage(id, boundToday)` / `boundDays(id)` — 연속 예속 일수.
  오늘 예속 = 추종 중 && 소유 타일 0 && 나를 따르는 자 없음.
- `SocialRank.BOUND_DAYS = 5` — 천민 판정 문턱. 농노도 이 값을 쓴다.
- `FarmEconomy.progressiveFee(...)` — 자산 누진 지대(시그모이드). 중간점 `WEALTH_MID_DAYS = 1.67`,
  기울기 `FEE_CURVE_K = 0.35`, 하한 0.15 · 상한 0.95. 가난하면 소작 몫 58%, 부유해지면 20% 아래로 마른다.
- `Tribute.due(follows)` — 인두세 1.0/일. `FoodEconomy` 번식 게이트 — 아래 §7.

## 2. 신분 판정 (파생 · 밤 1회 · 저장 없음)
```
지주    : ownedTiles(id) > 0
유생    : degree(id) >= 학사            (밭 없어도 예속되지 않는다)
농노    : owedOf(id) > 0 && boundDays(id) >= 5
자유소작 : 나머지 (무토지·무학위·빚 없음 또는 예속 5일 미만)
```
- **유생은 예속 일수를 세지 않는다.** `noteBondage` 호출부에서 학위자를 제외한다 → 가난해도 농노로 떨어지지 않는다(잔반).
- 농노 판정은 **한 번의 사고로 되지 않는다.** 빚이 남은 채 5일 연속이어야 한다. 능력자는 그 안에 갚고 빠져나온다.

## 3. 빚이 쌓이는 길 (= 농노로 가는 유일한 입구)
- **세금 미납** — 이미 `ledger.record(id, patronId, 0.0, arrears, day)` 로 빚이 된다. 그대로 쓴다.
- **위급 구휼** — 지금은 `record(id, ownerId, units × W_RELIEF, 0.0, day)` 라 **전액 은혜**이고 빚이 0이다.
  이것을 **절반은 은혜, 절반은 빚**으로 바꾼다: `record(id, ownerId, units × W_RELIEF × 0.5, units × 0.5, day)`.
  영주가 먹여 준 만큼 신세를 지고, 그 절반은 갚아야 할 것으로 남는다.
- **채무 상속** — 부모가 죽으면 `AllegianceStore.succeed` 가 채권·채무를 상속인에게 옮긴다(이미 있다).
  농노의 자식은 **농노로 태어나는 것이 아니라 빚을 물려받는다.** 능력 있는 자식은 갚고 벗어난다.
- **자립심(SELF_RELIANT)** 은 구휼을 받지 않는다(이미 구현). 빚을 안 져 예속도 안 되지만 굶어 죽을 수 있다 — 자유의 대가.

## 4. 상환 (= 능력이 탈출 속도를 정한다)
- 소작 몫에서 **빚이 있으면 자동으로 일부가 상환**된다: `min(몫 × 0.25, owed)`.
  수확이 많을수록 빨리 갚힌다. 부지런·유능·눈썰미는 며칠, 게으름·무능은 영영.
- 밤 정산의 기존 상환 자리(`TAX_SUM[3] 상환`)에 이어 붙인다.

## 5. 신분별 차이 (여기서만 갈린다)
### 지대 — 누진 곡선의 중간점을 옮긴다
`progressiveFee` 가 중간점을 인자로 받게 고치고(지금은 상수), 신분별로:
- 농노 **1.0** — 곳간이 조금만 차도 수취가 세진다. 축적이 막혀 예속이 이어진다.
- 자유소작 **1.67** — 현행 그대로. 경제 수준의 기준선이라 건드리지 않는다.
- 유생 **2.5** — 여유가 생겨도 한동안 제 몫을 지킨다. 재산을 모아 지주로 오른다.
### 세금
- 농노는 **인두세 면제**(`Tribute.due` 가 신분을 받아 0을 돌려준다). 지대로 이미 다 낸다.
- 자유소작·유생은 현행 1.0/일.
### 구휼 (쌍무성 — 몫이 적은 대신 굶지 않는다)
- 이체 상한을 농노 `min(3.0, larder − 예비)`, 나머지 `min(2.0, ...)`.
- 위급자 정렬에서 농노를 먼저.
### 배정 순서 (부역)
`assignDawn` 후보 정렬 키 맨 앞에 신분을 넣는다: 농노 0 → 자유소작 1 → 유생 2.
그 뒤는 현행(재고용 우선 → 빈곤 우선 → 거리 → id).
### 이동
- 농노: 원거리 이주 소작해제(128블록)·출근불능 예약석 반납(2일)·기근 이주 **미적용**.
- 자유소작·유생: 현행.

## 6. 벗어나는 길 (밤 정산 · 로그 "속량")
1. **상환** — `owedOf == 0` 이고 곳간 여유 ≥ 가구 하루소모 × 2.
2. **학교** — 학력 초급(1) 이상이 되는 날 즉시.
3. **군역** — 병사로 5일 연속 배속.
4. **개간** — 제 밭을 얻으면 지주. 단 농노의 개간 문턱은 ×2(영주 허락에 해당).
떨어지는 날은 로그 "전락"(빚 N · 예속 5일), 벗어나는 날은 "속량"(사유).

## 7. 번식 — 층마다 다른 이유로 많이 낳는다
현행: `larder − BIRTH_COST(3.0) − 가구하루소모 × REPRO_NEED_DAYS(2.0) ≥ 0`.
지금은 여유가 있어야만 낳으므로 **가난한 층은 아예 번식하지 못한다.**
`REPRO_NEED_DAYS` 를 상수에서 **개체 특성 함수**로 바꾼다:
- 무책임·멍청·현재지향·즉흥적 계열 발현 → **1.0** (생각 없이 낳는다)
- 과한책임·미래지향·준비성·신중 계열 발현 → **3.0** (확실할 때만 낳는다)
- 아무것도 없으면 **2.0** (현행)
결과: 부유층은 여유로 꾸준히 낳아 **적은 수의 가문이 중상층**을 잡고, 빈곤층은 앞을 안 보고 낳아
**많은 수의 후손이 하층**을 채운다. 그 아이들 일부는 굶고 일부는 빚을 물려받아 농노가 된다.

## 8. 로그·검증
- 밤 한 줄 — `[신분] 지주 N · 유생 N · 자유소작 N · 농노 N · 무직 N | 빚 합 X · 평균 예속일 Y`
- 사건 — "전락", "속량(사유)", "상환".
- 볼 것 ① 농노 비율이 무토지 성년의 절반~4분의 3인가 ② 능력 상위 개체가 농노로 떨어지는 비율(오탐)
  ③ 속량 사유 분포 ④ 세대별 후손 수가 하층에서 더 많은가 ⑤ 아사가 늘지 않는가(구휼 쌍무성이 받쳐 주는가).

## 9. 손 볼 특성 후보 (확정 전)
- **농노로 미는 쪽**: 멍청 · 무능함 · 게으름 · 무책임 · 현재지향 · 근시안 · 즉흥적 · 사치 · 낭비선호 ·
  의탁 · 안분지족 · 안분 · 무욕 · 산만 · 단순무식
- **많이 낳게 하는 쪽**: 다산 · 번식선호 · 다산선호 · 빠른육아 · 조혼
- **자유·상층으로 미는 쪽**: 명석 · 유능함 · 눈썰미 · 손재주 · 부지런 · 미래지향 · 천리안 · 준비성 ·
  검소 · 인색 · 자립심 · 야망가 · 자수성가 · 끈기 · 깜냥 · 만혼 · 번식불호 · 소산선호

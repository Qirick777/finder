# EvoSim 미검증 백로그 (게임 내 실증 대기)

> 순수 코어는 `/evotest all`(127/127)로 헤드리스 검증 완료. 아래는 **표현층 — 게임을 켜야만
> 확인되는 것들**로, 코드 리뷰까지만 끝난 상태다. 검증 시 이 파일의 상태 칸을 갱신한다.

## ★ 원트랙 일괄 검증 — `/evosim checkall` (권장 진입점)

명령 하나로 **37단계**를 차례차례: 각 단계가 "발동 직전" 조건을 자동 조성 → **결과값 변화만으로**
✅/❌ 자동 판정(호출 여부·로그 아님 — 미실행 오탐 없음) → 끝에 요약. 초원(풀 있는 곳)에서 실행.
단계: ①입금·인출 ②나눔 ③번식+출산비용(+베리) ④육아급식 ⑤R6귀가 ⑥R6강행(풀 필요) ⑦아사클럭
⑧이주(캐러밴·유아업기) ⑨⑩구혼여행(이동→성사) ⑪중혼 성사(관용 아내) ⑫중혼 거절(인색 아내 —
금지 결과 감시) ⑬노년 전이·자연사 ⑭노인 공유(책임: **40블록** 자식 집 저장고 0→2 — 마실 앵커 실검증)
⑮노인 무공유(무책임 — 금지 결과 감시) ⑯마실 육아(노인만 곁, fastCare 유아 생존 유지 — 금지 결과=아사)
⑰집앞 즉석 인출(H0.7→≥1.5·저장고 3→2) ⑱혼인우선 가장(성년 아들 동거 + 번식 회계 항등식)
⑲전투 도주 해제(좀비 원격화 후 3블록 내 귀가 실측) ⑳제자리 합의 기각(새집이 합의지에서 32+)
㉑아이들만 이주 금지(금지 결과 감시) ㉒㉓낮샘플 육아(압축 시계 600으로 실경로 완주 — 곁 생존/방치 아사)
㉔원장 등록·사망(실개체 등록→파괴→사망일, cleanup이 debugRemove로 회수) ㉕무대 제외(원장 등장 금지)
㉖㉗날로먹기(인출 0.7→~1.9 / 입금 2.3→~1.05·저장고 3→4 — 일반값과 수치로 구분) ㉘㉙힘 공격 속성
(힘센V=일반×1.4 / 약함V=×0.7 즉시 실측) ㉚㉛무모 번식 완화(저장고 11 경계: 무모② 출산 / 일반 대조군
금지 감시) ㉜㉝조심성 좀비 유인(noAi 결정론: 10블록에서 조심성 반경9 안 노려짐 / 일반 반경12 노려짐)
㉞밭 무단·슬롯0(부재지주 9타일 금지 감시) ㉟지대 정산(계정 2.7→저장고 +2·0.7 이월 회계)
㊱밭 상속(아들 승계 ∧ 자기 소작 해소) ㊲만족 정지(부유 무동기 지주 노동 금지 감시).
checkall 실패 단계만 아래 개별 명령으로 재현·역추적한다.

### 영문 결과 로그 (AI 원인 규명용 — cmd 한글 깨짐 회피)
- 스텝 판정마다 **cmd 콘솔**(ANSI 색: 성공 녹/실패 적)과 **`<서버폴더>/evosim-verify.log`** 파일에 영문 1줄:
  `[VERIFY 12/33] FAIL polygamy_reject | reason=forbidden_result | elapsed=14.2s | bride married ... | expect: bride must stay single (...)`
- reason 코드: `result_met`(성공) / `timeout`(제한 초과) / `forbidden_result`(금지 결과 발생) /
  `no_forbidden_result`(금지 감시 통과) / `setup_error`·`judge_error`(예외 — 조성/판정 국면 구분).
- 끝에 `[VERIFY SUMMARY] n/33 PASS, k FAIL: slug, ...`. 슬러그는 코드 위치와 1:1 — **이 파일을 그대로
  붙여넣으면 실패 원인 역추적·수정이 가능**하다. 단독 명령(LiveCheck)도 `[VERIFY-LIVE]` 접두로 동일 기록
  (라벨은 한글 유지 — 토큰 PASS/FAIL/reason은 영문이라 파싱 가능, 슬러그 영문화는 후속).

## 검증 명령 작성 규칙 (불변 원칙 — 모든 신규 검증은 이 목록을 통과해야 한다)

1. **결과값 원칙**: pass 는 오직 상태·수치의 *변화*로 판정한다. 호출·로그·시도를 성공 조건으로 쓰지 않는다.
2. **직전 조성**: setup 이 "발동 직전" 상태를 강제로 만든다(외부 상태 가정 금지). 게이트 값은 경계가 아니라 여유 통과/여유 미달로.
3. **위양성 차단**: 대상이 아닌 우회 경로가 같은 결과를 낼 수 있으면 그 우회를 세팅으로 봉쇄(예: 나눔 검증의 저장고 0). 봉쇄 불가하면 잔존 확률을 주석에 수치로 명시.
4. **위음성 차단**: 경계 진동·자연 소모·이동 시간을 감안한 안정 판정식(예: R6 의 H≥1.2 && L 감소)을 쓴다.
5. **금지 결과는 passOnTimeout**: "안 일어나야 성공"은 시간 감시로 뒤집어 판정한다.
6. **격리**: 무대 간 상호작용 차단 — 슬롯 격리(checkall) 또는 시작 정리(discardFamily·debugClearBerries) + 판별 인수(LiveCheck.cancelAll). **같은 자리 2회 연속 실행이 항상 같은 판정**이어야 한다.
7. **정리**: 종료(성공/실패/중단) 시 스폰 개체·파생 개체(자손)·월드 잔재(블록·저장고 엔트리·이주 합의)를 회수한다.
8. **지형 독립**: 거처 좌표는 heightmap 보정(groundAt/ground). 환경 의존(풀 필요 등)은 안내문에 명시.
9. **시간 훅 규칙**: 절대시계 로직은 가속 불가 — 검증용 압축 훅(fast*, careTimeScale)은 *주기·창만 압축*하고 판정 결말 코드는 실경로와 공유해야 한다(별도 구현 금지). 훅이 우회하는 구간을 주석에 명시.
10. **판정식-코드 대칭**: 기대값이 코드 공식에 의존하면 같은 공식으로 회계 대조(berryCost 패턴). 상수는 하드코딩 대신 상수 참조 — 상수 변경 시 판정이 자동 추종.
11. **커버리지 의무**: 새 기능/수정마다 ⓐ순수부 evotest ⓑ표현부 checkall 스텝(또는 단독 명령) ⓒ즉시 검증 불가면 이 문서에 한계·관찰 방법 등록 — 셋 중 어디에 속하는지 커밋에 명시.
12. **원트랙 편입**: 신규 스텝은 checkall 에 편입하고, 단독 명령은 실패 재현·역추적용으로 유지한다.

## A. 수치 자동 판정 명령 (결과가 숫자·✅/❌로 출력)

| # | 검증 대상 | 명령 | 성공 기준(자동 판정) | 상태 |
|---|---|---|---|---|
| 1 | 입금·인출 귀가 | `/evosim trip` | 남편 H 2.50→[1,2) · 아내 H 0.70→≥1.5 (30초 내, 2초마다 수치 중계) | 미실시 |
| 2 | 가족 나눔(B) | `/evosim share` | 아내 H 0.25→≥0.6 · 남편 H≤1.8 (20초 내) | 미실시 |
| 3 | 번식 게이트+출산비용 | `/evosim birth` | 즉시: 저장고 20→(17−베리비용) 회계 일치 · 유아 정확히 1명 | 미실시 |
| 4 | 육아 급식(D) | `/evosim care` | 즉시: 유아 H 0.50→1.50 · 저장고 5→4 | 미실시 |

## B. 로그·육안 판별 명령

| # | 검증 대상 | 명령 | 성공 신호 | 상태 |
|---|---|---|---|---|
| 5 | R6 위급 분기 | `/evosim r6` | 밤에 A(저장고0): 안 자고 [채집]→[회복] / B(저장고3): 귀가 [인출]→[회복] | 미실시 |
| 6 | 이주 전체 | `/evosim exodus` | 즉시 [이주]×3(첫 건만 "길잡이") · 유아 업기(다둥이 분산+홀아비) · [폐가]×3 · [건축완료] · 도착 시 유아 하차 | 미실시 |
| 7 | 구혼 여행(족외혼) | `/evosim suitor` | 즉시 [구혼여행] 출발 → 64블록 동쪽 직행 → [구애] 성사 → [짝성립] → 정착 | 미실시 |
| 7b | 중혼(일부다처) | `/evosim polygamy` | 신부 혼인+합류 수치 자동판정(관용 아내·저장고 40 — 부양선 18 여유). 거절 케이스는 checkall ⑫ | 미실시 |
| 8 | 베리 심기·수확 실연 | `/evosim berry` | [베리]+N → 익음 → [수확] (30초 실연) | 미실시 |
| 9 | 장기 밸런스 관찰 | `/evosim food` | 가계/인구 시계열 축적 → evosim-events.log 통째 분석(BASE_YIELD 등 [미확정] 튜닝 근거) | 미실시 |
| 10 | 무대 자동판정 회귀 | `/evostage all` | 전 무대 성공 — 특히 reproduction(잉여 20.0)·cycle_starve(새 아사 경로) | 미실시 |

## C. 부수 확인 (별도 명령 없음 — 위 실행 중 함께 관찰)

| # | 항목 | 확인 방법 | 상태 |
|---|---|---|---|
| 11 | 신규 로그 실출력(등장·구애·가계·인구·동원·입금·인출·위급 등) | 아무 명령 후 `/evolog dump 30` | 미실시 |
| 12 | `/evolog food` 즉석 스냅샷·`/evolog`(상태) | 직접 실행 | 미실시 |
| 13 | 검사봉 표시(보유 H·저장고) | 미믹 우클릭 | 미실시 |
| 14 | NBT 영속(H·아사클럭·이주 필드·저장고·이주합의·외로움클럭·짝까다로움 슬롯·조상명단) | 아무 명령 후 재접속 → 수치 유지 + 재접속 후 `/evosim suitor`(모닥불 목록 복구 확인) | 미실시 |
| 15 | LiveCheck 판별기 자체(중계·타임아웃) | 1·2번 실행으로 겸증 | 미실시 |
| 16 | 성별 채집 배율 실효(남1.5×/여0.5×) | 9번 로그의 [채집] +량 비교 | 미실시 |
| 17 | 폐가 저장고 유산 계승(애향심 재사용) | exodus 후 `/evosim reuse` 조합 | 미실시 |
| 18 | 굶주림→아사 연속 피해 | 10번 cycle_starve 무대가 커버 | 미실시 |
| 19 | 육아 성별 자연선택(남무심·여적극 수렴 가설) | 9번 장기 관찰 — 매일 `인구` 로그의 `육아성향 남N·여N` 시계열에서 남성 육아성향이 세대에 걸쳐 감소하는지 | 미실시 |
| 20 | 노년 전이·자연사 | `/evosim elder` (checkall ⑬) — 노년 경유+사망 상태 실측 | 미실시 |
| 21 | 노인 공유·마실 육아 | `/evosim eldercare` (checkall ⑭~⑯) — 자식 집 저장고 수치·유아 생존 | 미실시 |
| 22 | 청년 35일·노년 8일 실주기(비가속) | 9번 장기 관찰 — `성장 성년→노년`·`자연사` 로그 시각 간격 실측 | 미실시 |
| 23 | 전 시스템 감사 수정분(베리 저장고 실차감 · 거처중심 가구 스캔48 · 건축중 대표 제외 · 집앞 즉석 인출 · 혼인우선 가장 선출 · 중혼 실시간 부양판정 · 재혼 시 자식 동반 이주 · 배우자 생존 반경128 · 구혼여행 실패 반나절 재시도/만료 시 폐가→방랑 · 이주 성년가드·정착클럭 성년기준·제자리 합의 기각 · 노인 마실 앵커·허기 중단·노년 건축/업기) | checkall 재실행 + 9번 장기 로그([베리] 비용 표기·[구혼여행] 만료·과부가구 소멸 여부) | 미실시 |
| 24 | 가계 링크 확실화(모성애 캐시=각 자식의 친어미 PA/PB · 육아 로그 친어미 귀속 · 이주 업기 친어미 우선 · R4 동원 기준=가장 특성 · 재혼 거처 처분을 거처중심 "다른 동거 성년·노년" 기준으로 — 원거리 재혼 시 아들·노부모 있는 집 폐가화 방지) | checkall ③④⑧ 재실행 + 장기 로그에서 [육아] 귀속·이주 업기 어미 확인 | 미실시 |
| 25 | 직계 근친 차단(조부모·증조-손주 금지, 조상 명단 병합 저장 — 사촌 허용 유지) + 영속 보강(짝까다로움 MChM/MChF·외로움클럭·모닥불 목록 리로드 복구) + 과부가구 로그 남성 유무 정밀화 + 노년 전이자의 구혼여행 잔재 정리 | 순수부는 evotest mating/직계차단 ✅ 완료 · 게임부는 재접속 후 구애/구혼여행 관찰 | 부분완료 |
| 26 | 노년=나이 분리(가장·아내 포지션은 혼인 링크로 유지: 노년 가장 급식 1순위·동원 기준·제공자 역할 유지, 노년 아내 중혼 게이트(상한·질투)·과부 승계 포함 — 번식만 청년기 한정) | checkall ⑬~⑯ 재실행 + 장기 로그에서 노년 전이 후 가계 로그의 급식·동원 정상 여부 | 미실시 |
| 27 | 재점검 수정 배치(가구 스캔 96+단독 이주 안전핀 · birth/checkall③ 재실행 잔재 제거(베리·개체) · 이주 합의 origin-dest 비교 기각 · 건축 중 정산 허용 · 베리 배율 가장 귀속 · 노년가구 로그 · 노년 사별 감지) | checkall 재실행(같은 자리 2회 연속 — ③ 재실행 안정성 포함) + exodus 후 재기근 시 [이주] 로그의 목적지 확인 | 미실시 |
| 28 | 지형 엣지 관찰(깊은 물 위 부유 흙판·MAX_FLATTEN 16 초과 절벽의 벽 결손 — 수정 보류: 실측 없이 부지 선정 휴리스틱 변경은 더 위험) + 기존기능 수정 배치 1~14(나눔 위협 양보·전투 해제·좀비 유인 차등·구애 쿨다운/눈낮춤·육아 낮 샘플·건축 4건·저장고 GC·SimEvents·수면/귀가·대사·판별 정리) | 물가·절벽 신축 관찰 + checkall 재실행 + suitor/polygamy 재확인(수락 확률 하향 영향) | 미실시 |
| 29 | 추가 스캔 일괄 수정(무대 인수 — 새 명령이 진행 중 판별을 명시적 중단·정리 · 단독 명령 전부 지형 높이 보정 · food/berry 시작 정리 · polygamy 자손 회수 · 애향 밀집 거리 12 복원) | 경사지에서 단독 명령 실행(천막 접지) + 판별 중 다른 명령 실행 시 "⏹ 중단" 출력 + 애향×애향 신축 간격 실측(~12) | 미실시 |
| 30 | 검증 보강 스텝 ⑰~㉓ 자체(집앞 인출·아들 동거·전투 해제·이주 가드 2종·낮샘플 육아 2종) + ⑭ 40블록화 + careTimeScale 훅 | checkall 재실행 — 신규 7스텝 ✅ 확인(⑲는 좀비 스폰 정상 여부, ㉒㉓은 판정 소요 ~각 20초) | 미실시 |
| 31 | 가계도·통계 GUI(순수 연산은 evotest lineage ✅ 완료 — 조상 그리드 배치·다이아몬드 후손 중복 제거). 게임부: ① 검사봉 쉬프트+우클릭 → 가계도 열림·부모/조부모 상자·연결선 ② 조상 상자 클릭 → 그 조상 포커스로 재조회(위로 항해) ③ `/evosim birth`로 태어난 무대 유아가 원장·통계에 **안 잡히는지**(무대 제외) ④ village 출산 후 `/evosim stats` 그래프·랭킹 반영, `/evosim legacy` 채팅 폴백 일치 ⑤ 재접속 후 죽은 조상이 가계도에 그대로(원장 NBT 영속) | 육안 — 규칙 11-ⓒ(GUI는 수치 판정 불가, 서버 계산부는 원장 그대로라 데이터는 evotest 커버) | 부분완료 |
| 32 | 특성 반대급부 개편(순수부는 evotest reproduction/준비임계·multiplier 4b·food/절약특성·날로먹기·combat/유인배율·physique/힘트레이드오프 ✅ 완료). 게임부(표현층 배선 실효): ① 식물혼동 개체가 18블록(기본 12) 동물에 반응 ② 피공포·공간지각 개체의 풀 탐색 반경 확장(±7~10, 기본 ±5) ③ 조심성 개체는 좀비 유인 9블록(기본 12 — wildpairs 밤 관찰로 어그로 시점 비교) ④ 힘센V 개체의 좀비 처치 타수 감소(공격 1.4×)·약함V 증가 ⑤ 날로먹기 개체 H 회복이 인출 1회당 1.2(검사봉 수치) ⑥ 구세이브 로드 시 舊 요리치 보유 개체가 날로먹기로 표시(NBT 별칭) | 육안+검사봉 — 순수 수치는 전부 evotest 커버, 표현층은 반경·속도라 수치 명령 부적합(규칙 11-ⓒ) | 부분완료 |

## ★ 이연된 인게임 관문 (봉건 밭 경제 — 전 단계 완성 후 일괄 실행)

M-단계 육안/실측 관문은 사용자 지시로 **M8 완성 후 종합 체킹**으로 이연한다(빌드업 순서의
"앞 단계 관문 통과 후 착수" 규칙의 예외 — 단, 실패 시 해당 M부터 역추적).

> 관문(기능 발동)과 별도로 **장기 동역학 대조는 [PREDICTIONS.md](PREDICTIONS.md)** 를 따른다 —
> 아키타입별 예상 궤적(day별 밭·인력 규모)·판정선(F1~F6)·설계 리스크(R1~R6). `/evolog on` 후
> `evosim-events.log`를 예측 표와 대조해 "기능은 되는데 설계가 안 굴러가는" 결함을 가른다.

일괄 실행 목록:
- [ ] M0 `/evosim farm 25` — 고랑 수열·발자국 육안 + `/evosim farmclear`
- [ ] M1 `/evosim farmown` — 자영 수확 LiveCheck(H+3·잔여익음≤3, verify.log)
- [ ] M2 `/evosim farmhire` — 새벽 배정→소작 수확(이웃 H↑ ∧ 지대 계정↑, LiveCheck)
- [ ] M2 `/evosim farmguard` — 부재지주 9타일: 슬롯0(<최소일감)·무단 수확 금지(익은 9 유지, 금지 감시)
- [ ] M3 `/evosim farmrent` — 밤 지대 이체(저장고 3→5 정수만 ∧ 계정 2.7→0.7 이월, LiveCheck)
- [ ] M4 `/evosim farmbond` — 3일째 새벽 실경로 배정에서 상시 승격(tenantFarm=구획, LiveCheck)
- [ ] M4 `/evosim farmshield` — 위급 상시 소작 구제(H≥1.0 ∧ 영주 저장고 3→2 ∧ 관계 유지)
- [ ] M4 `/evosim farmbreak` — 영주 저장고 0 → 구제 불이행 해제(tenantFarm=0)
- [ ] M4 `/evosim farmseat` — 예약석: 일용 슬롯 0(9타일·ΣC24)에서도 상시 소작 배정 유지
- [ ] 밭 NBT 영속: farmown 실행 → 재접속 → 구획·타일·계정·TenantFarm 유지(항목 14와 병행)
- [ ] 통근 초과 해제: 상시 소작을 60블록 밖 텔레포트 → 다음 새벽 [소작해제] 로그·bond 소멸
- [ ] M5 `/evosim farmgrow` — 지대 재투자(R1): 타일 9→11 ∧ 계정 7→0(확장 6 차감 후 잔여 1 밤 이체)
      ∧ 주인 저장고 20→21 ∧ 소작 저장고 16 불변(소작농 주머니는 손대지 않음)
- [ ] M5 `/evosim farmfound` — 신규 개간: 구획 +1(3타일 착공) ∧ 지주 저장고 40→10
- [ ] M5 `/evosim farmcap` — 능력 게이트: 무능력 지주 33→정확히 35 정지 ∧ 저장고 30→24
- [ ] M6 `/evosim farminherit` — 지주 파괴 → 아들 승계 ∧ 아들의 자기 소작 해소
- [ ] M6 `/evosim farmvacant` — 무후 지주 파괴 → 무주 → 밤에 이웃 선점(vacantSince 해제)
- [ ] M6 관찰: 무주지 만료 소거(2.5일 — 이웃 없이 방치 후 등록 소멸·야생 베리 잔존)
- [ ] M7 `/evosim farmidle` — 만족 정지: 부유 무동기 지주(저장고 60)는 익은 9타일을 안 딴다(금지 감시)
- [ ] M7 `/evosim farmdrive` — 대조: 부지런 지주는 만족 무시하고 수확(ripe 감소)
- [ ] M7 관찰: 경쟁 배회 노동(wildpairs에서 경쟁 개체가 배회 시간에도 채집 — 풀 필요)·신설 3축
  유전 발현(village 후 검사봉으로 야망가/욕심/사치 확인)
- [ ] M8 관문: checkall **37단계** 재실행(㉞~㊲ 밭 스텝 포함, 같은 자리 2회) + `/evosim lords`
  랭킹 출력 + 검사봉 HOME 모드의 밭 줄(구획·타일·지대·소작·만족) 확인

### 밭 관문 진단 지도 (결과 수신 즉시 원인 특정용 — 실패 슬러그 → 의심 지점 → 다듬기 방향)

| 실패 슬러그/증상 | 1순위 의심 지점 | 다듬기 방향 |
|---|---|---|
| farm 수열이 눈에 이상(고랑 없음/비정사각) | `FarmLayout.layout` 규칙(폭<min(2K−1,cap)) | evotest farm/배치수열은 통과 상태이므로 **월드 사상**(`buildDemoPlot`의 row×2 오프셋·groundAt 경사 보정) 쪽 |
| farmown FAIL reason=timeout, H 정체 | `MimicFarmGoal.canUse`(WORK 판정·용량 리셋) → goal 등록 순위(6) 경합 | progress의 ripeLeft가 줄었는데 H만 안 오르면 `addHarvest` 배율, ripeLeft 그대로면 goal 미발동 — Schedule/우선순위 |
| farmown H는 오르나 잔여익음>3 | 용량 C(12) 소진 후 정지 — 정상 동작과 판정 기준 충돌 | 판정 완화가 아니라 타임아웃 내 재개 여부 확인(다음날 용량 리셋) |
| farmhire assigned=no 지속 | `FarmTicker.assignDawn` 게이트(시각창 1000~9000·하루 1회·후보 필터) | progress에 assigned 표기 있음 — no면 배정부, yes인데 H 정체면 `nearestWorkRipe`(배정 구획 스캔) |
| farmhire assigned=yes, H↑, rent=0 | 소작 분기(`plotOf`가 null → 자영 처리) | 타일 색인/plotOf 선형 탐색 — 좌표 as Long 불일치(groundAt 재보정 여부) |
| farm_guard_no_poach FAIL(익은 수 감소) | ①일반 채집 가드(`MimicForageGoal.farmTile`) 누락 경로 ②배정 오발(부재지주 ΣC=0인데 need 9<10 계산 오류) | verify.log의 H 수치로 구분: H 크게 오름=수확자 있음→가드, H 미동=블록만 소실→외부 요인(좀비 등) |
| farm_rent_settle 저장고 5 미달 | `settleRent` 게이트(tod≥13000·하루 1회·주인 탐색) | account가 그대로면 정산 미발동(시각/rentDay), account만 줄고 저장고 그대로면 홈 좌표 불일치(`debugSettleWithTent` 홈과 get/set 좌표) |
| farm_rent_settle 계정 0.7 아님 | 정수 이체 회계(`floor`·이월) | 0이 됐으면 소수까지 이체(P2 위반 — L 정수성 확인), 2.7 그대로면 미발동 |
| farm_bond_promote streak 안 오름/1 고정 | `assignDawn` 연속일 분기(LAST_ASSIGNED 롤오버) | streak 1이면 시드 유실(clearAssignments 순서), 2 고정이면 새벽 게이트(시각창·assignDay) 미발동 |
| farm_bond_promote streak 3인데 tenantFarm 0 | 승격 분기(`getTenantFarm()==0`·PROMOTE_DAYS) | setTenant 호출 지점만 점검 |
| farm_shield_relief H↑인데 larder 3 유지 | R6 자가 회복(판정이 이미 차단) → 구제 미발동 | `protectTenants` 게이트(200틱 스캔·isCritical·주인 홈 탐색) |
| farm_shield_relief bond broken | 구제보다 해제 선행(저장고 판독 시점) | larder 조성값·홈 좌표(get/set) 일치 확인 |
| farm_shield_break bond kept | 해제 분기(larder<1) 미도달 | 스캔 주기·주인 홈 null 경로 확인 |
| farm_grow_reinvest 소작 저장고 감소 | 자금 분기(`growFarms`의 hasTenant → plot.account) | 소작농 주머니 지불 = R1 재투자 회귀(구 코드 잔재) |
| farm_grow_reinvest tiles 9 유지 ∧ 계정 7 유지 | 성장 게이트(시각창 13000·growDay·유주택 소작 탐색·주인 만족 캐시) | 계정도 그대로면 미발동. 주인 satisfiedToday 가 true 로 남았는지 먼저 확인 |
| farm_grow_reinvest tiles 11 ∧ 계정 1 유지(이체 안 됨) | 호출 순서(`onServerTick`: growFarms→settleRent) 또는 `settleRent` 주인 탐색 | 확장은 됐는데 잔여 정수가 안 넘어감 = 순서 회귀 |
| farm_grow_reinvest 계정 음수 | `reinvestTiles` 상한(floor) vs 설치 좌표 수 불일치 | placed>afford 면 isLoaded 건너뜀 회계 결함 |
| farm_found_new owned 0 유지 | 신규 분기(자금 게이트 40≥36·부지 탐색 findFarmSite) | larder 불변=자금/시각 게이트, larder만 감소=부지 실패 후 차감 순서 결함(치명 — 즉보고) |
| farm_skill_cap tiles 36+ | `growthCap`/`canManageLarge` 게이트 누락 | 능력 목록에 기본 특성이 섞였는지(STRONG 등은 비능력이어야 정상) |
| farm_inherit_son owner 그대로(사망자 id) | remove() 상속 훅 미발동 또는 inherit 조기 반환 | discard의 destroy 여부·ownedCount 대조 |
| farm_inherit_son owner 0(무주) | 후보 탐색(부모 링크 스캔) 실패 | spawnChildOf의 parentAId 설정 확인 |
| farm_inherit_son sonTenant 유지 | 자기 소작 해소 분기 | 승계는 정상 — 해소 줄만 점검 |
| farm_vacant_claim owner 0 유지 | 선점부(통근·유주택) 또는 밤 게이트(growDay) | vacantSince set이면 상속 정상 — 선점 루프만 |
| farm_idle_satisfied FAIL(ripe 감소) | satisfied 미설정(새벽 updateMotivation 미발동) 또는 FarmGoal 만족 게이트 | progress의 satisfied가 no면 갱신부(시각창), yes인데 땄으면 goal 게이트(배정 예외 경로) |
| farm_drive_diligent ripe 9 유지 | Satisfaction의 DILIGENT 오버라이드 또는 goal WORK 판정 | satisfied yes면 오버라이드 누락(순수부 — evotest 대조), no인데 안 따면 M1 계열(farmown 진단행) |
| 공통: setup_error | 무대 조성부(스폰 실패·지형) | 슬러그 무관 — groundAt 결과 좌표와 예외 메시지 우선 |

실패 보고는 verify.log 원문이면 충분 — 슬러그·reason·수치 3요소로 위 표를 탄다.
- [ ] 동시: checkall 33단계(같은 자리 2회), TESTING 항목 30·31·32, evosim-verify.log 제출

## 실패 시 절차

실패한 명령 이름 + 채팅 판정 줄(또는 `/evolog dump 30` 출력)을 그대로 전달 → 수치로 역추적.

## [미확정] 밸런스 상수 (9번 로그로 실측 후 확정)

BASE_YIELD(2.0) · BIRTH_COST(3.0) · FILL_TARGET(1.5)/RETURN_LOW(0.8) · GRACE_TICKS(1200) ·
FAMILY_TICK_INTERVAL(1200) · 아사 피해량(0.5/100틱) · STARVE_WINDOW(1일)/RESETTLE_COOLDOWN(2일) ·
LONELY_TRAVEL_AFTER(3일)/TRAVEL_DURATION(2일) · comfort(하루소모×2) ·
돌봄 샘플 창(1000~12000)/간격(400틱)/하루 1회 래치 기준 — 육아 자연선택압 실측(19번과 연계) · ACCEPT_SCALE(0.7)/거절 쿨다운(1일)/눈낮춤 k−거절수 ·
특성 반대급부(전부 신규 — 인구 시계열로 도태 여부 실측): 무모 임계 −1/신중 +1/하한 0.5 ·
탐지 배율(동물 1.5·식물 피공포 1.5·공간지각 1.25·시너지 1.5)/피공포 감지 +3 · 눌변가 노동 +0.1 ·
절약 소모(아이불호·번식불호·빈약 0.95·병약 0.9) · 날로먹기 섭취 1.2↔저장 0.8(왕복 96%) ·
조심성 유인 0.75 · 힘 공격 +8%/약함 −6%·소모 ±4%/등급(V=종전 고정값)

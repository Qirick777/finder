# 추종·신분 체계 업데이트 계획서

> **이 문서는 업데이트 완료 시 삭제한다.** 진행 중 임시 문서이며, 확정된 설계는
> 구현과 함께 각 클래스 주석과 `TESTING.md` 로 옮긴다.

---

## 0. 달성 목표 (합격 판정의 최종 기준)

| # | 목표 | 판정 방법 |
|---|---|---|
| 1 | 엘리트가 최상위 계층이 되며 세력이 난립하지 않는다. 인위적으로 추가된 엘리트만 도달 가능 | 최상위 세력 수 = 1, 그 주인이 엘리트 |
| 2 | 밭은 흔하되 마구 커지지 않는다. 엘리트의 밭만 매우 커진다 | 구획 수↑, 평균 타일↓, 최대 타일은 엘리트 소유 |
| 3 | 출산률 정상 — 대다수 1명, 여유 있으면 여러 명 | 가구당 자녀 분포, 인구 증가 곡선 |
| 4 | 모든 기능이 정상 작동하고, 지배자는 손해가 아니라 **이익**을 본다 | 시설·군인 수지가 지배자 기준 흑자 |
| 5 | 군인이 굶지 않고 잘 흩어져 경비를 선다 | 군인 아사 0, 군인 간 최소거리 분포 |
| 6 | 군인이 밤에 자지 않는다. 위협받는 미믹이 있으면 즉시 출동 | 야간 활동률, 위협 발생→출동 지연 시간 |
| 7 | 엘리트가 죽어도 추종이 사라지지 않고 승계·축적된다 | 승계 전후 세력 크기 유지 |
| 8 | 추종자의 자식은 자연히 부모와 같은 추종 대상을 갖는다 | 신생아의 추종 대상 일치율 |
| 9 | 일반민도 세력을 만들 수 **시도**할 수 있으나 능력이 안 돼 불가능하다 | 문턱 시도 건수 > 0, 성공 0 |

---

## 1. 확정 사항

### 1.1 구속 — 신세 장부 하나

```
신세[A→B] = 탕감분(무상) + 상환분(빚)
```

| 항목 | 분류 | 상환 | 추종 점수 |
|---|---|---|---|
| 위급 무상 구제 | 탕감분 | 불필요 | ○ (가중치 최대) |
| 식량 대출 | 상환분 | 이자 有 | ○ |
| 세금·사용료 미납 | 상환분 | 이자 有 | ○ |
| 혼인 주선비 | 상환분 | 이자 有 | ○ |

- **추종 성립** = 신세 합계가 최대인 상대, 단 임계 초과 시.
- **임계는 자기 재산에 비례** — 같은 은혜라도 가난한 자에게만 구속이 된다(규칙5: 신분 분기 금지, 비율로 갈린다).
- **매일 갱신되는 계약이다.** 추종자가 위급인데 주인이 방치하고 타인이 구제하면 신세가 그쪽으로 옮겨가 **추종 대상이 바뀐다.** 이것이 지배의 유지 비용이자 지배자 간 경쟁의 장이다.
- **천민** = 상환분/상환능력이 임계 초과 상태로 지속. 추방이 아니라 **영구 예속**이며 보호는 계속 받는다(주인에게 손실이 아니라 값싼 노동이므로 논리가 닫힌다).

### 1.2 계층 — 전부 파생값, 저장하지 않는다

`왕 / 차상위 지배 / 일반 / 천민`

- **일반과 천민의 차이는 저축이 쌓이는가 하나뿐**이다. 하는 일은 같다.
- 일반 = 잠재성이 있는 자, 천민 = 잠재성이 소진된 자.
- 왕 = 아무도 추종하지 않으면서 지배자들을 거느린 자(간접 지배). 복수 허용 —
  거리로 갈린 영역이면 자연스럽고, 같은 영역이면 하극상으로 정리된다.
- **이탈은 "나감"이 아니라 "올라섬"이다.** C→A→B 가 되면 A 는 중간 계층이 된다.
  완전 이탈은 채무 청산 + 세력 역전(하극상)일 때만.

### 1.3 승계 — 밭 승계와 같은 단계

- 아버지 사망 시 **장남**이 밭과 함께 **채권(받을 신세)과 채무(진 신세)를 모두** 승계.
- 채권 승계 → 아버지의 추종자들이 아들을 따른다 = **왕조**
- 채무 승계 → 아버지의 예속이 아들에게 = **농노 세습**
- 첩 자식은 상속 순위에서 **본처 자식 뒤**. 다만 **태생적 추종자**로 남아 세력의 인적 기반이 된다.
- 신생아는 부모의 추종 대상을 그대로 물려받는다(목표 8).

### 1.4 밭 — 상한을 세력에 연동한다

```
밭 상한 = 기본 상한 + 추종자 수 × 계수
```

이 한 줄이 목표 1·2·9 를 동시에 해결한다.

- 밭을 **여는 것**은 쉬워진다(착공 문턱 완화) → 밭이 흔해진다.
- 밭을 **키우는 것**은 추종자를 거느려야 가능하다 → 엘리트만 크게 키운다.
- 일반민은 밭을 열 수 있으나 키울 수 없다 → 목표 9 의 "시도하나 능력이 안 됨".

### 1.5 서비스 — 추종자 전용, 사용료 있음

| 시설/직 | 대상 | 효과 | 대가 |
|---|---|---|---|
| **군인** | 반경 내 추종자 | 몹 방어 | 세금 |
| **학교** (`school.nbt` 21×21×18) | 추종 가구의 소년 | **획득** 능력치(유전 안 됨) | 소액 수업료 |
| **교회** (`church.nbt` 15×33×18) | 확률적 방문자 | 주인: 신세 감쇠 완화 / 개체: 구애 우위 | 소액 헌금 |

- **자선이 아니다.** `수입 = 사용료 × 이용자`, `비용 = 건축비(1회) + 종사자 급여(매일)`.
  이용자가 많을수록 이익이므로 **세력이 클수록 남는 장사**이고, 작은 세력은 적자라 시설을 못 짓는다.
- 사용료를 못 내면 **상환분 적립** → 예속 심화. 시설은 예속의 도구다.

### 1.6 군인 — 전업

- **주간 훈련 / 야간 순찰.** 반농반병이 아니다(계층이 뚜렷해야 하고, 부양 문턱이 곧 세력 자격이므로).
- **배치는 어두운 칸 기준.** `/evosim lamps` 의 `밝기0(몹 생성 가능) N칸` 을 그대로 위험 지도로 쓴다.
  자동으로 흩어지고, **가로등이 없는 곳에 사람이 선다** — 인프라와 인력이 같은 지표를 공유하므로
  지주가 둘 중 어디에 투자할지 고르게 된다.
- **장비**: 철검 + 사슬 갑옷. 전투력 보정 + 시각적 식별. 드롭 방지는 기존 `setDropChance(…, 0.0F)` 패턴.
- **야간 대기.** 위협 발생 시 즉시 출동(목표 6). 자면 안 된다.
- 먹어야 싸운다 — 체력 회복은 소지 식량(`getHolding`) 소비. **잉여 식량이 곧 군사력이다.**

### 1.7 천민의 진로

- **남성**: 군인 / 농노 — **결핍이 큰 쪽으로 배분**
  (일손 부족 = `workedTiles < tiles` 이면 농노, 어두운 칸이 많으면 군인)
- **여성**: 남는 수는 **첩**. 상한은 **거처 수용 여유** = `Tier.capacity − (본인+본처+자녀)`
  (소형 4 · 중형 6 · 대형 9 · 저택 12). 별도 상한 상수가 필요 없다.

### 1.8 기타 확정

- **소년 이동속도** `SurvivalRules.moveSpeedFactor(BOY)` **0.6 → 0.8**
  (학교를 매일 왕복해야 하므로 통근으로 하루가 끝나면 안 된다)
- **등하교**: 채집·밭일 시간 시작 시 학교로, 종료 시 귀가. 기존 `MimicLeashGoal`(출근) 재사용.
  학교에 못 가는 소년은 기존대로 `MimicPlayGoal`(놀이) → **눈으로 구분된다.**
- **교회 방문**: 구애·마실 시간에 확률적. 하루 방문 상한으로 몰림 방지. `MimicVisitGoal` 재사용.
- **보호는 거리 의존** → 추종자가 주인 근처로 이사 → **세력별로 마을이 뭉친다**(공중사진 검증).
- **떠나면 추종은 자연히 끊긴다** — 거리 기반 보호의 실익이 사라져 감쇠가 빨라진다. 별도 규칙 불필요.
- **시설 종사자(교사·성직자·군인)는 일반 계층.** 급여를 받고 세금도 낸다.
- **난이도**: 개발·검증은 `peaceful`, **최종 확인만 `easy`**.

---

## 2. 단계 분할

각 단계는 **독립적으로 검증 가능**해야 하고, 그 단계가 주장하는 것만 측정한다.

### P0 — 기반 정비
**내용**
- 빈집 결함 수정 (실측: `등기44(빈집44)` 인데 `거주가구24` — 월드 이어 띄운 뒤 거주 연결 이상 의심)
- 소년 이동속도 0.6 → 0.8

**검증**: 회귀 가드만. `등기 = 거주 + 빈집` 항등 성립, 소년 통학 가능 거리 확인.
**합격**: 빈집 수치 정합, 회귀 가드 통과.

---

### P1 — 경제 재조정 (목표 2, 3)
**내용**
- 밭 착공 문턱 완화 (밭이 흔해지도록)
- 밭 상한 도입: `기본 + 추종자 수 × 계수` — 이 단계에서는 추종자가 0 이므로 **전원 기본 상한**
- 출산 조건 정상화

**검증**
| 지표 | 기대 |
|---|---|
| 구획 수 | 증가 |
| 평균 타일 | 감소 |
| 최대 타일 | 기본 상한 근처에서 정지 |
| 가구당 자녀 분포 | 대다수 1명 이상 |
| 인구 곡선 | 증가 추세 |

**합격**: 위 다섯 + 회귀 가드.
**주의**: 이 단계에서는 **엘리트도 상한에 걸린다.** 정상이다 — P3 에서 추종자가 생기면 풀린다.

---

### P2 — 신세 원장 (행동 영향 0)
**내용**
- 신세 장부(탕감분/상환분), 이자, 감쇠, 개체당 상위 K 유지
- 무상 구제·대출 기록
- 추종 판정 **파생만** (행동은 아무것도 바뀌지 않음)
- `/evosim allegiance` 보고 신설

**검증**
| 지표 | 기대 |
|---|---|
| 신세 간선 수 | 0 아님 |
| 추종 성립 수 | 0 아님 |
| 추종 집중도 | 소수에게 집중되는가 |
| 순환 | 0 |
| 그 밖 모든 지표 | **P1 과 동일** (행동 영향 0 이므로) |

**합격**: 신세가 쌓이고 추종이 성립하며, **다른 모든 수치가 P1 과 같다.**
이 "같아야 한다"가 이 단계의 핵심 검증이다.

---

### P3 — 계층 파생 + 승계 (목표 7, 8)
**내용**
- 계층 판정(왕/차상위/일반/천민) — 파생값
- 채권·채무 승계를 밭 승계에 부착
- 신생아가 부모의 추종 대상을 계승
- 밭 상한의 추종자 연동이 여기서 실제로 작동하기 시작

**검증** — 사망이 필요하므로 **D25 이상** 필요
| 지표 | 기대 |
|---|---|
| 계층 분포 | 피라미드 형태 |
| 승계 발생 건수 | > 0 |
| 승계 전후 세력 크기 | 유지 (목표 7) |
| 신생아 추종 일치율 | 높음 (목표 8) |
| 엘리트 밭 최대 타일 | 기본 상한 **초과** (목표 2) |

---

### P3.5 — 신분 4계층의 단일 파생 (행동 영향 0)
**왜** — P3 의 계층 판정에 결함 셋이 있었다.
1. **천민이 도달 불가능한 죽은 가지**였다. 판정식이 상환분에만 의존하는데 상환분은 대출·세금이
   없어 구조적으로 0 이다(P3c D14 실측 `상환분합0`). 0 으로 찍히는 것이 측정 결과가 아니었다.
2. 판정식이 **보고 명령 안에만** 있어 게임 로직이 읽을 수 없었다. P4~P7 이 전부 이것을 입력으로
   쓰므로 두 벌로 갈라질 자리였다.
3. 라벨에 **계측이 없어** 계층이 실제로 갈리는지 볼 수가 없었다.

**내용**
- `SocialRank` — 지배/상위/평민/천민. 판정식의 단일 출처. 저장하지 않는 파생값
- **연속 예속 일수**를 원장에 추가 — 주인이 있고 제 땅이 없는 상태의 연속 수. 천민 판정의 주 척도
- **연속 궁핍 일수**도 남긴다 — 계측 전용(아래 "버린 척도" 참조)
- 보고에 계층별 평균(소유 밭·살림·추종자 수) + 척도 자체(예속 최장·궁핍 최장·상환분합) 추가

**계층 정의** (전부 셀 수 있는 수 — 신분을 묻는 항목 없음)
| 계층 | 조건 |
|---|---|
| 지배 | 주인 없음 · 추종자 ≥1 · 추종자 중에 또 주인이 있음(간접 지배) |
| 상위 | 추종자 ≥1 (지배가 아닌 자) |
| 천민 | 추종자 0 · 주인 있음 · 소유 타일 0 · (상환분 > 상환능력 또는 **연속 예속 ≥5일**) |
| 평민 | 나머지 |

**버린 척도 — 궁핍**: 처음에는 "가구 저장고 < 가구 하루소모"가 사흘 연달으면 천민으로 잡으려
했는데 **측정에서 0 이 나왔다**(D14: 계층별 평균 살림 20 · 재산 최소 10 — 가장 가난한 가구조차
하루치가 있다. P2 에서 구휼 0건이던 것과 같은 사실). 그것은 "굶는가"를 묻는 척도인데 천민의
정의는 "벗어나지 못하는가"였다 — 다른 질문을 재고 있었다. 숫자를 깎아 발동시키는 대신 척도를
바꿨고, 궁핍은 "아무도 굶지 않는다"를 계속 보이는 계측으로만 남겼다.

**검증**
| 지표 | 기대 |
|---|---|
| 계층 분포 | 천민 > 0 — 그리고 0 이면 척도 줄(예속 최장·문턱 도달 인원)이 **왜 0 인지** 말해야 한다 |
| 계층별 평균 소유 밭 | 지배 ≫ 상위 > 평민 ≥ 천민 |
| 행동 영향 0 | **런 대조가 아니라 호출 그래프로 증명한다**(아래) |

**주의 — 런 대 런 비교는 성립하지 않는다.** `EvoSimCommand.scatter()` 가 `level.random` 을
쓰는데 `ServerLevel.random` 은 월드 시드가 아니라 실행마다 새로 뽑는 씨앗을 쓴다. 시드를 고정해도
1일차 배치부터 달라진다(P3c D1 촬영중심 `@3,1` 대 P3.5 D1 `@-10,-10`). 따라서 "다른 지표가
그대로"를 런 두 개의 숫자를 견주어 주장하면 안 된다 — **P2b 때의 그 주장도 같은 결함을 안고
있었다.** 행동 영향 0 은 새 값을 **읽는 곳이 보고뿐임**을 호출 그래프 전수로 보여 증명한다.

---

### P4 — 세금·상납·미납 (목표 4) — **완료**
**내용**
- 세금 징수(추종자 1인당 정액), 상납 사슬(하위 지배자 → 왕), 미납 → 상환분 적립
- 상환분에 이자. **기존 결함 수정**: `decayDaily` 가 탕감분과 상환분을 똑같이 깎아,
  미납이 쌓이기 시작하면 빚이 저절로 탕감되는 셈이었다
- **시설 원장은 P5 로 미뤘다** — 읽는 곳이 없는 장부는 검증할 수 없고, P4 검증표는
  전부 세금만으로 채점된다

**실측 결과**
| 지표 | 기대 | 실측 |
|---|---|---|
| 지배자 수지 | 흑자 | **달성** — 상위 1인 +2.05(D10) → +8.67(D16) → +16.69(D19). 세력에 비례해 커진다 |
| 미납으로 인한 천민화 | 발생하되 폭주 아님 | **경로 실증** — D10 천민 2명 중 1명이 미납 경로(예속 5일 이상은 1명뿐) |
| 계층별 살림 | (P3.5 에선 차이 없었음) | 119 / 65 / 33 으로 완전히 갈림 — 세금이 부를 위로 옮긴다 |
| 인구 | 세금이 잡아먹지 않을 것 | D19 인구 69, 세금 없던 직전 런보다 많다(런마다 배치가 달라 인과 주장은 불가) |
| 상납 사슬 깊이 ≥ 2 | — | **P4 범위 밖. 아래 참조** |

**"상납 사슬 깊이 ≥ 2" 를 P4 에 넣은 것은 계획 순서의 착오다.** 원장에 간선을 만드는 경로가
소작·구휼·긴급고용 셋뿐인데 셋 다 **가난한 쪽이 받는 것**이라, 지주가 다른 지주에게 신세를 질
길이 없다. 지주를 채무자로 만드는 항목 — 시설 사용료(P5·P6) · 혼인 주선비 · 보호(P7) — 은
전부 다음 단계에 있다. 세율이나 임계를 만져 만들 문제가 아니므로, **이 항목은 P5 로 옮긴다.**

**상납 코드 자체는 검증했다** (`/evosim bondtest` — 시험 전용, 자연 경로 아님).
세력 1·2위 사이에 임계(43.2 = 216타일 × 0.2)를 넘는 신세 52.8 을 심었다.

| 시점 | 사슬깊이 | 신분 | 상납 | 계층별 1인 순수지 |
|---|---|---|---|---|
| 심기 전 | 1 | 지배0 상위3 | 0.0 | — |
| 심은 직후(같은 날) | **2** | **지배1** 상위2 | 0.0 (일일 패스 전 — 정상) | — |
| 새벽 넘긴 뒤 | 2 | 지배1 상위2 | **10.5** | **지배 +30.53 · 상위 +6.39 · 평민 −0.34 · 천민 −1.02** |

산술 검증: 2위가 걷은 약 21 의 절반이 10.5 로 정확히 올라갔고(`TRIBUTE_SHARE` 0.5),
1위 순수지 +30.53 ≈ 자기 징수 20 + 받은 상납 10.5 이다. 값이 0 이 아닌 것에 그치지 않고
**뺄셈이 맞는다.** 간선 하나로 계층 파생(`SocialRank.RULER`)이 즉시 반응하는 것도 함께 확인됐다.

**보고 읽을 때 주의**: 세수·순수지는 그날치만 메모리에 담긴다. 서버를 막 켠 직후 스냅숏은
세금이 안 걷힌 것처럼 0 으로 보인다 — 결함이 아니라 집계 범위의 성질이다.

---

### P5a — 시설 등기·장부 + 학교 건축 — **완료**
**내용**: `FacilityTemplate`(시설 저작 규약) · `FacilityStore`(등기+장부) · `considerFacility`
(착공 판정) · `/evosim facilities`

**시설 저작 규약** — 학교·교회 도면에는 거처의 금블록 앵커도 스위트베리 6개도 없다. 남의 도면을
우리 사정에 맞춰 고치는 대신 별도 규약을 세웠다: **종(bell)이 정확히 1개**이고 그것이 건물
한가운데 꼭대기라, 종의 x·z 열 바닥면 한 칸 위가 앵커다. 좌표 변환·계단 반전 보정은 거처와
**같은 함수**를 쓴다(두 벌로 갈라지면 시설에서만 계단 모서리가 어긋난다).

**실측(D24, 인구 105)**
| 지표 | 결과 |
|---|---|
| 학교 | 2~3채 · 구조 일치율 **100%** · 무너짐0 · 주인사망0 |
| 착공 문턱 | 자격자 중 일부만 착공(나머지 자금 부족 보류) — 난립 없음 |
| 밭·집 무결성 | 몸통갈린0/22 · 고랑오염0 · 밭겹침0 · 밭까지 2칸 이하 0채 |
| 빠진칸 | 전부 사유 확인(`집여유`/`시설여유`/블록명) — **설명 안 되는 칸 0** |

**건물은 즉시 세운다.** 1,336칸을 거처와 같은 박자로 지으면 성인 둘이 나흘 반을 밭·채집에서
빠져 그 자체가 마을을 굶긴다. 대신 건축비를 실제로 저장고에서 뺀다(인부를 산 값).

**이 단계에서 잡은 결함 셋**
1. **시설 자격을 가구가 아닌 개인으로 판정** — 정산 대표는 UUID 최소로 뽑혀 사실상 무작위라,
   영주의 배우자가 대표면 그 가구는 영영 자격이 없다. 최대세력 13 인데 학교 0채·사건 0건이었다.
2. **밭·집이 시설을 몰랐다** — 밭이 학교 위로 자라도, 학교 위에 집을 지어도 막을 것이 없었다.
   첫 학교가 밭에서 멀리 선 것은 운이지 안전이 아니다.
3. **방향 전환이 축(dir 비트2)까지 넘었다** — 전환 이전 타일은 옛 격자에, 이후는 새 격자에
   놓여 어느 축으로도 직사각형이 아니게 됐다. 실측: 채움 **133%**(불가능) · 고랑오염 34/72 ·
   덩어리9. P3c 의 인접 조건은 연결성은 보장했지만 격자 일관성은 보장하지 않았다.
   축은 착공 때 한 번 정하고 그 뒤로는 부호만 튼다. 같은 사건(방향전환) 앞뒤 대조:
   채움 133%→51% · 구멍 32→0 · 줄끊김 25→0 · 덩어리 9→1 · 고랑오염 34→0.

**계측에서 배운 것** — "0 이 나왔을 때 왜 0 인지 보고가 스스로 말해야 한다"가 이 단계에서
세 번 반복됐다(궁핍0 · 학교0채 · 구멍). 세면서 좌표를 안 찍으면 원인을 영영 못 본다. 그리고
**보고는 배치와 같은 코드에 물어야 한다**(`FarmTicker.plantBlockReason`) — 규칙을 보고 쪽에
베껴 쓰면 두 벌이 갈라진다.

**이월 관찰**: 구획 간 최소거리가 1.0 까지 내려왔다(맞닿은 구획쌍 1). 착공은 기존 밭을
피하지만 **확장에는 구획 간 간격 규칙이 없다.** 규모가 커지며 처음 도달한 상태이고, 각 구획은
반듯하다(덩어리1). 손대지 않고 기록만 남긴다 — P5b 와 함께 흔들면 귀속이 흐려진다.

---

### P5b — 등하교 + 수업료 + 신세 + 획득 능력치
**내용**: 교사 배치·급여, 추종 가구 소년의 등록·등하교, 수업료(미납 → 상환분),
등교로 쌓이는 신세, 획득 능력치(유전 안 됨)

**왜 이것이 사슬을 붙잡는가** — P4·P5a 실측에서 **지배 계층이 생겼다 사라졌다** 한다
(D18 지배1·깊이2 → D20~22 지배0·깊이1). 지주 간 신세는 하루 5% 씩 옅어지는데 채워줄 경로가
없어서다. 원장에 간선을 만드는 셋(소작·구휼·긴급고용)은 전부 가난한 쪽이 받는 것이고 지주는
어느 것도 받지 않는다. **학교는 사슬을 새로 만드는 장치가 아니라 이미 생긴 사슬을 고정하는
못이다** — 밭을 가진 자도 신세가 `타일×0.2` 를 넘으면 주인을 갖는데, 그 지주의 아들이 주인의
학교에 다니면 매일 신세가 채워져 감쇠를 이긴다.

**검증**
| 지표 | 기대 |
|---|---|
| **상납 사슬 깊이** | **≥ 2 가 여러 날 연속 유지** (P4 에서 옮겨옴 — 한 시점 관측은 무효) |
| 등교 소년 / 전체 소년 | 추종 가구 비율과 일치 |
| 통학 거리 분포 | 0.8 속도로 왕복 가능한 범위 |
| 능력 격차 | 등교/비등교 간 벌어짐 |
| 학교 수지 | 흑자 (지금은 순 −60 — 건축비만 물린 상태) |

**측정으로 걷어낸 것 세 가지** (전부 "기능이 굶고 있던" 원인이지, 규칙의 문제가 아니었다)

1. **진입 칸이 앞마당 한복판이었다.** `길놓기 학교 경로 없음 — 고립 부지` 를 부지가 멀어서라고
   의심했는데 틀렸다. 도면을 재니 학교는 문(z −6) 앞에 제 앞마당이 z −10 까지 깔려 있고,
   진입 칸은 "문 밖 한 칸" 이라 z −7 — 사방이 건물 제 바닥이라 **이웃 열림 0/4** 였다.
   길 찾기가 첫 칸도 못 떼니 부지를 어디에 잡든 결과가 같았다. 진입 칸을 점유 열 밖까지
   내보내 학교·교회 모두 (0,−11) · 이웃 열림 3/4 가 됐다. **교회도 같은 결함을 갖고 있었다.**
2. **소년기가 1.25일이었다.** 소년으로 사는 날이 하루뿐이면 등교 가능일도 하루라
   `schoolDays` 가 쌓일 수 없다. 3일(72000틱)로 늘렸다 — 대가로 인구 곡선을 함께 봐야 한다
   (먹기만 하는 기간이 길어지고 성년 충원이 늦어진다).
3. **통학 한계 32 가 등록을 0 으로 만들었다.** 21×21 건물이 집 간격(15~19)보다 넓어 마을
   안에 못 들어가고, 가장자리에 서면 32 안에 드는 아이가 없었다(실측 0/6명 · 0/9명).
   48 로 되돌리고 **등록과 실제착석을 나눠 찍는다** — 둘의 차가 곧 닿지 못한 아이다.

**아직 손대지 않고 기록만 하는 것**: `FacilityTemplate.groundCols()` 는 모든 Y 를 합쳐
342칸을 장애물로 잡는데 그중 **69칸은 지붕·처마뿐인 열**이다. 거처는 "처마 밑 지면은 길이
지나가도 된다"(`HomeBlueprint.groundFootprint` 문서)로 규칙이 반대다. 이번 결함의 원인은
아니므로(최하층만 봐도 진입 칸은 0/4) 한 번에 하나씩 검증하려고 남겨 둔다.

---

### P6 — 교회
**내용**: `church.nbt` 건축, 성직자 배치, 확률적 방문, 감쇠 완화 + 구애 우위

**검증**
| 지표 | 기대 |
|---|---|
| 일일 방문자 분포 | **몰림 없음** (상한 내 고르게) |
| 감쇠 완화 효과 | 방문자의 신세 유지율↑ |
| 교회 수지 | 흑자 |

---

### P7 — 군인 (목표 5, 6)
**내용**: 군인 직무(주간 훈련/야간 순찰), 검+사슬갑옷, 어두운 칸 분산 배치, 즉시 출동, 급식

**검증 — 2단**
- **(a) `peaceful` 에서**: 배치·급식·야간 활동만 확인
  | 지표 | 기대 |
  |---|---|
  | 군인 아사 | 0 |
  | 군인 간 최소거리 | 뭉치지 않음 |
  | 야간 활동률 | 취침 아님 |
  | 배치 칸 vs 어두운 칸 | 일치 |
- **(b) `easy` 에서**: 전투만 확인
  | 지표 | 기대 |
  |---|---|
  | 위협 발생 → 출동 지연 | 짧음 (목표 6) |
  | 추종자 몹 사망 | 비추종자보다 낮음 |
  | 군인 전사율 | 보충 가능한 수준 |

---

### P8 — 통합 검증 (목표 1, 9 + 전체)
**내용**: 구현 없음. 문턱 상수 확정(측정 후) + 목표 1~9 전수 확인.

| 목표 | 판정 |
|---|---|
| 1 | 최상위 세력 = 1, 주인이 엘리트 |
| 9 | 일반민의 문턱 시도 > 0, 성공 0 |
| 그 밖 | 각 단계 지표 유지 |

**최종은 `easy` 난이도.**

---

## 3. 회귀 가드 (모든 단계에서 매번 확인, 저렴하게)

| 항목 | 기준 |
|---|---|
| 아사 | 0 |
| 인구 | 증가 추세 |
| 서버 예외 | 0 |
| 밭 형태 | 구멍·줄끊김·고랑오염 0, 테두리 결손·침범·몸통내잔여 0, 몸통갈린구획 0 |
| 부지 | 물 위 0, 물가 0, 밭 위 0, 거처 위 소거 0 |
| 집–밭 거리 | 최소 3 이상, 2칸 이하 0채 |

하나라도 깨지면 **그 단계에서 멈추고 원인을 찾는다.** 다음 단계로 넘어가지 않는다.

---

## 4. 측정 후 결정할 상수 (규칙 14 — 감으로 정하지 않는다)

| 상수 | 측정 대상 |
|---|---|
| 밭 기본 상한 · 추종자당 계수 | P1 에서 밭 분포, P3 에서 엘리트 밭 성장 |
| 신세 임계 · 감쇠율 | P2 에서 추종 성립 수와 집중도 |
| 이자율 · 복리 상한 | P2 에서 부채 나선 발생 여부 |
| 군인 1명 부양비 (= 세력 문턱) | P7 에서 부양 가능 인원 |
| 세율 · 사용료 | P4 에서 지배자 수지 |
| 교회 방문 확률 · 일일 상한 | P6 에서 몰림 여부 |
| 소년 통학 허용 거리 | P5 에서 실제 통학 거리 분포 |

---

## 5. 테스트 원칙

1. **그 단계가 바꾼 것만 본다.** 전 항목 재측정은 하지 않는다. 회귀 가드만 매번 돌린다.
2. **"변하지 않아야 한다"도 검증이다.** P2 처럼 행동 영향 0 인 단계는 *다른 수치가 그대로인 것*이 합격 조건이다.
3. **조건이 재현되지 않으면 "미검증"이라고 보고한다.** 사건이 발생하지 않은 것을 합격으로 세지 않는다.
4. **계측기를 먼저 의심한다.** 이 프로젝트에서 계측기 자체의 버그가 네 번 나왔다(축 맹목 2회, 하이트맵 4회, 정원/밭 혼동 2회).
5. 관측 런은 배율 ×6 (×16 은 `TickAccel` 과부하 가드에 상시 걸려 사실상 1배).

---

## 6. 삭제 조건

목표 1~9 가 P8 통합 검증에서 전부 통과하면 이 문서를 삭제하고,
확정된 설계는 각 클래스 주석과 `TESTING.md` 로 옮긴다.

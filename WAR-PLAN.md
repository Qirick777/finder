# 세력 통합 계획서 — 압박·전투·점령

> **이 문서는 구현 완료 시 삭제한다.** 진행 중 임시 문서이며, 확정된 설계는
> 구현과 함께 각 클래스 주석과 `TESTING.md` 로 옮긴다.
>
> 선행 완료: 추종 3단계(신뢰/충성/종속) · 구걸 · 거지 계열 특성 · 군인 적합도.
> 이 문서는 그 위에 얹는다.

---

## 0. 왜 만드는가

지금은 **제 세력권 안에 다른 독자세력이 생겨도 아무 일도 일어나지 않는다.** 보호세를
걷는 대상이 `followerHomesOf(주인)` — 즉 **이미 내 사람**뿐이라, 옆집 영주는 세금도
안 내고 빚도 안 지고 영원히 독립이다.

국가란 한 영역 안에서 **보호를 독점한 세력**이다. 그 독점이 성립하는 과정을 넣는다.

### 설계 원칙

**신분으로 분기하지 않는다.** 아래 어디에도 "지배자면 ~한다"가 없다. 전부 세는 수 —
병사 수 · 타일 수 · 연속 일수 · 체력 · 저장고 — 로만 갈린다.

**무장 여부가 두 경로를 가른다.**

```
막사 없는 독자세력  →  압박으로 흡수     (비폭력)
막사 있는 독자세력  →  전투로 주권 탈취  → 그 뒤 압박으로 흡수
```

---

## 1. 확정 설계

### 1.1 압박 — 비무장 세력 흡수

**표적**: 막사 경계 반경(`COMMUTE_RANGE 96`) 안에서
① 추종자가 있고 ② 주인이 없고 ③ **제 막사가 없는** 자.

**행동**: 병사의 순찰 경로에 그 집을 넣는다. 병사가 실제로 가서 선다 — 육안으로 보인다.

**정산**: 그날 병사가 그 집에 닿았으면, 표적 → 막사 주인 방향으로 신세 적립.

```
W_PRESSURE = 1.5 / 일   (탕감분. 감쇠 0.95 · 적립 체감 ACCRUE_FREE 12 ~ CAP 60 적용)
```

**왜 1.5인가.** 굴복 문턱은 `max(MIN_BOND 4, 타일 × TILE_WORTH 0.2)` 이고, W=1.5 는
체감식에서 균형점을 **23.1** 에 세운다:

| 상대 규모 | 문턱 | 결과 |
|---|---|---|
| 30타일 | 6 | 약 4일에 굴복 |
| 60타일 | 12 | 약 10일에 굴복 |
| 115타일 | 23 | 경계선 — 사실상 안 넘어감 |
| 200타일 | 40 | **압박만으로는 영영 굴복 안 함** |

**대영주는 협박으로 안 넘어온다.** 작은 독립세력은 압박으로 접히고, 대등한 상대는
전쟁으로만 갈린다. 두 경로를 나눈 이유가 이 산술이다.

**빚(상환분)이 아니라 탕감분인 이유**: 탕감분은 감쇠한다. 막사가 무너지거나 병사가
이탈해 압박이 끊기면 **굴복이 풀린다.** 빚은 이자로 불어나 영구화되어 반란이 구조적으로
불가능해진다.

**굴복 후**: 그의 추종자가 통째로 딸려 와 사슬깊이 2 → 막사 주인이 `SocialRank.RULER`.
**밭은 그대로 그의 것이다.**

### 1.2 전투 — 무장 세력 간 주권 다툼

**교전 조건**: 병사 A와 B가 서로의 `Combat.detectionRange` 안에 있고, 두 막사 주인이
다르며, 어느 쪽도 상대를 따르지 않을 때.

**전투 판단은 새로 만들지 않는다.** 이미 있는 것을 표적만 바꿔 쓴다:

```java
Combat.entry()            겁쟁이=FLEE · 용감=인지범위에서 선공 · 중립=붙으면 반격
Combat.retreat()          무모=HOLD(안 물러남) · 신중=가족 곁이면 HOLD · 그 외 HP≤30% 퇴각
Combat.returnsToCombat()  신중만 HP≥70%에 재진입
```

바꿀 곳은 `MimicCombatGoal` 의 `nearestMonster(...)` 한 줄이다.

**누가 죽는지는 특성이 정한다** — 대부분 패주로 끝나고, 죽는 건 **무모한 자**다.
새 부상 상태를 만들지 않는다: `전투불가 = 사망 또는 HP ≤ RETREAT_HP(30%)`.

**점령**:

```
OCCUPY_DAYS = 2
```

연속 2일 동안 그 막사에 전투 가능한 병사가 하나도 없고 적 병사가 반경 안에 있으면
→ **막사 주인 변경**. 하루 운으로 주권이 넘어가지 않고, 증원이 도착할 시간이 생긴다.

**패자는 땅을 안 뺏긴다.** 잃는 건 주권(막사)뿐이고, 그 다음 날부터 1.1의 압박 표적이
되어 사람이 넘어온다.

### 1.3 증원 — 국력을 전선에 도달시킨다

**문제**: 정원이 `그 막사 근처 추종 가구 / HOUSEHOLDS_PER_SOLDIER(4)` 라, 세력 전체의
부와 인원이 전선에 못 온다. 국력이 압도해도 국지에서 운으로 진다.

**해소**: 교전 중인 막사에만, 같은 주인의 다른 막사에서 파견.

```
GARRISON_MIN = 2   (각 막사가 남겨둘 최소 수비. 초과분만 파견)
```

이동은 **구걸에서 검증한 장치를 그대로 쓴다** — 96블록 경유지(`BEG_STEP`) · 리시 호위 ·
마감 만료. 223블록 완주를 실측한 코드다.

봉급도 그만큼 나가므로, 감당 못 하면 이탈로 저절로 줄어든다 — **억제가 자동이다.**

### 1.4 후송과 급양 — 지배자의 지갑이 곧 전투지속력

**절반은 이미 되어 있다.** `MimicEntity.regenTick()` 이 이미 식량에 물려 있다:

```java
if (isUnderThreat()) return;   // 전투 중엔 회복 없음
if (holding <= 0.0)  return;   // 굶는 중엔 회복 없음
heal(REGEN_BASE * Physique.recovery(individual));
```

연결할 것은 **급양 경로 하나**뿐이다.

```
① 부상   HP ≤ RETREAT_HP(30%)          → 전투 이탈
② 후송   가장 가까운 <b>아군</b> 막사로 이동 (제 소속 무관)
③ 급양   영주 저장고 → 병사 H 에 MEDIC_RATION 지급
④ 회복   regenTick 이 H>0 를 체력으로 바꾼다 (기존 코드)
⑤ 복귀   HP ≥ RETURN_HP(70%)           → 제 자리로
```

```
MEDIC_RATION = 2.0   (후송 1회당)
```

**아군 판정**: 두 막사 주인이 같거나, 추종 사슬을 타고 올라간 **최상위 주인이 같으면**
아군. `AllegianceStore.patronMap` 으로 구한다.

**이것이 점령 조건을 만든다.** 칼이 아니라 곳간으로 이긴다:

> 적의 지갑이 마르면 → 급양이 끊기고 → H가 0이라 회복이 멈추고
> → 부상병이 계속 전투불가로 남고 → **2일 뒤 막사가 넘어간다**

**수치 근거**: 회복은 40틱마다 0.5 HP. 30%(6 HP) → 70%(14 HP) 는 약 640틱(32초).
**시간은 병목이 아니고 급양이 병목이다.** 병사 12명이 하루 한 번씩 후송되면 하루 24 —
관측된 영주 저장고가 60~130 이니 부자는 버티고 빈자는 며칠 만에 마른다.

**부수 효과**: 전방 막사가 값어치를 갖고(후송 거리가 짧아 회전이 빠르다), 부상병 행렬이
눈에 보인다(스크린샷 판정 가능).

> **장비 등급안은 폐기했다.** 연속 완납 일수로 가죽→사슬→철을 나누려 했으나, 후송·급양이
> 같은 일("부가 화력이 된다")을 더 적은 기전력으로 한다. 장비 등급 · 승급 비용 ·
> 연속 완납 카운터가 통째로 사라진다.

### 1.5 전사자 유족 보상

전사 시 그 가구 저장고에 지급한다.

```
DEATH_BENEFIT_DAYS = 7.0
지급액 = 가구 성인 명목 하루소모 × 7.0   (영주 저장고에서, 부족하면 있는 만큼)
```

성인 2인 가구면 약 11~16 — 다른 부양자를 찾을 일주일치다.

**이 지급은 구휼로 기록한다**(`W_RELIEF` 경로). 유족이 영주에게 신세를 지므로
**전쟁 손실이 오히려 가문을 결속시킨다.**

> 군인 선발 조건에 `isProviderRole()` 이 있어 **모든 군인은 가구 부양자**다. 전사 1명은
> 가구 1개의 붕괴다. 그것은 전쟁의 대가이지 결함이 아니다 — 이 보상은 완충이지 면제가
> 아니다.

---

## 2. 작업 분할 — 단계마다 단독 검증

각 단계는 **그 앞 단계 없이도 검증 가능**하도록 잘랐다. 순서를 지키는 이유는 의존이
아니라 **위험 순**이다 — 가장 무효화 위험이 큰 것을 먼저 확인한다.

### P0. peaceful 난이도에서 몹 간 피해 확인 ★최우선

관측 런은 `difficulty=peaceful · spawn-monsters=false` 다. **몹 간 피해가 이 설정에
걸리면 2~7 단계가 통째로 무의미하다.**

- 조성: `evosim hittest` — 미믹 둘을 붙여 놓고 한쪽이 `doHurtTarget` 을 부르게 한다
- 합격: 상대 HP 가 실제로 준다
- 불합격 시: 난이도를 바꿔야 하고, 그건 세계 전체 거동이 바뀌는 일이라 **먼저 보고하고
  승인을 받는다.** 임의로 바꾸지 않는다

### P1. 상수 정의

`Facilities` 에 5개. 코드 변경 없음, 컴파일만 확인.

```
W_PRESSURE = 1.5 · OCCUPY_DAYS = 2 · GARRISON_MIN = 2
MEDIC_RATION = 2.0 · DEATH_BENEFIT_DAYS = 7.0
```

### P2. 유족 보상 (독립)

- 전투 없이도 검증 가능(아사·수명으로 죽는 군인으로 확인)
- 합격: 군인 사망 시 그 가구 저장고 증가 + `구휼` 신세 기록 + 로그 1줄
- 검증: `evosim wartest` 없이, 인구 런의 자연 사망으로 관측

### P3. 압박 순찰 + 신세 (독립 — 전투 불필요)

- 순찰 경로에 표적 추가, 도달 시 신세 적립
- 합격: 조성한 독자세력 머리의 신세가 **하루 1.5씩** 오르고, 문턱을 넘는 날 굴복
- 검증: `evosim presstest` — 영주(막사 보유) + 독자세력 머리(막사 없음, 추종자 1)를
  경계 반경 안에 세우고 신세 곡선을 잰다. 예측 곡선과 대조

### P4. 전투 표적 확장

- `MimicCombatGoal` 이 적 병사를 표적으로 삼는다
- 합격: 두 세력 병사가 만나면 실제로 교전하고, 특성대로 갈린다
  (겁쟁이 즉시 도주 · 신중 HP30%에 퇴각 · **무모는 죽는다**)
- 검증: `evosim wartest` — 막사 둘을 겹쳐 세우고 사망/퇴각 분포를 잰다
- **여기서 무모 분포를 처음 실측한다** (사망률 예측의 유일한 근거)

### P5. 후송·급양 + 점령

- 부상병이 아군 막사로 후송 → 급양 → 회복 → 복귀
- 전투 가능 병사 0이 2일 지속되면 점령
- 합격: ① 부상병이 실제로 막사로 걸어간다 ② 영주 저장고가 급양만큼 준다
  ③ 저장고가 마르면 회복이 멈춘다 ④ 2일 뒤 막사 주인이 바뀐다
- 검증: `evosim wartest` 에 저장고 조작을 더해, **부자 vs 빈자**로 승패가 갈리는지 확인

### P6. 증원

- 교전 막사로 다른 막사에서 파견
- 합격: 파견 병사가 실제로 전선에 도착한다(구걸 이동 장치 재사용)
- 검증: 막사 3개(주인 A) vs 1개(주인 B) 조성 → A가 국지 수적 우위를 만드는지

### P7. 통합 관측 런

- 인구 런에 세력 둘을 심고 d20까지
- 합격 판정:
  - 인구가 접히지 않는다 (사망률 · 인구 곡선)
  - 밭 생산이 무너지지 않는다
  - **국력이 큰 쪽이 여러 날 단위로는 반드시 이긴다**
  - 통합 후 전쟁이 멎고 순찰만 남는다

---

## 3. 미리 밝히는 위험

| # | 위험 | 대응 |
|---|---|---|
| 1 | **peaceful 에서 몹 간 피해가 안 들어갈 수 있다** | P0 에서 가장 먼저 확인. 막히면 보고 후 승인 |
| 2 | **소모전으로 인구가 접힐 수 있다** | 퇴각이 기본이라 사망은 무모한 자로 한정되나, **무모 분포를 아직 안 쟀다**. P4 에서 실측 |
| 3 | 급양 무한 회전으로 전선 고착 | 증원(P6)이 승부를 낸다. 그래도 안 되면 `MEDIC_RATION` 을 올려 소모를 키운다 |
| 4 | 통합 완료 후 전쟁 소멸 | 국가의 완성이므로 결함은 아니다. 그 뒤 무엇을 볼지는 별도 주제 |
| 5 | 세력권 겹침 시 압박 표적 중복 | 압박은 **막사 없는 자**만 노리므로, 무장 세력끼리는 전투로 갈린다 — 중복이 생기지 않는다 |

---

## 4. 선행 작업 현황 (이 문서 착수 시점)

| 항목 | 상태 |
|---|---|
| 추종 3단계 (신뢰/충성/종속) + 적립 체감 | ✅ 커밋 `aa8bb75` — 체감식 실측 일치 |
| 구걸 (223블록 완주 · 수령 · 신세 상승) | ✅ 커밋 `aa8bb75` |
| 거지 계열 특성 (산만 · 단순무식 · 야성) | ✅ 커밋 `6814a78` — `/evotest beggar` 5/5 |
| 군인 적합도 (완력 + 경계) | ✅ 커밋 `6814a78` |
| 방패 = ARMOR +4 속성 | ✅ 커밋 `6814a78` |
| 순찰 반경 = `√(인지/8)` | ✅ 커밋 `6814a78` |
| **새 특성이 밭 성장에 주는 영향** | ⏳ 인구 런 관측 중 (D6 기준 24타일 — 지연되나 정지는 아님) |

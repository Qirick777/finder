package com.evosim.mod.entity;

/**
 * <b>시설의 수치</b> — 학교·교회의 문턱과 값. 한 곳에 모아 둔다.
 *
 * <p>전부 <b>측정 뒤에 확정할 값</b>이다. 판정 지표는 시설 수지(흑자여야 한다)와 시설 수
 * (0 도 난립도 아니어야 한다)이며, 0 이 나오면 사건 로그의 "보류/자리 없음" 줄이 <b>왜</b>
 * 0 인지 말해야 한다.
 */
public final class Facilities {

    private Facilities() {
    }

    /**
     * 학교 건축비 — 저장고에서 즉시 빠진다(인부를 산 값).
     *
     * <p>60 은 저택({@code MANSION.buildCost} 70) 바로 아래다. 학교는 1,336칸으로 저택
     * (1,027칸 중 비공기)보다 크지만, 저택은 <b>사는 집</b>이라 그 값 자체가 과시의 상한 노릇을
     * 한다. 학교를 그보다 비싸게 두면 저택을 지을 수 있는 자만 학교를 지을 수 있어 두 문턱이
     * 겹쳐 버린다 — 서로 다른 선택지가 되게 조금 아래에 둔다.
     */
    public static final double SCHOOL_COST = 60.0;

    /**
     * 학교를 세울 자격 — 거느린 자가 이만큼 있어야 한다.
     *
     * <p>계획서 1.5: "이용자가 많을수록 이익이므로 세력이 클수록 남는 장사이고, 작은 세력은
     * 적자라 시설을 못 짓는다." 그 문장을 그대로 수로 옮긴 것이 이 상수다. 6 은 실측(P4 D19)
     * 에서 주인 3명의 세력이 28·25·3 이었던 분포를 보고 잡았다 — 상위 둘만 자격이 되고
     * 말단 지주는 안 되는 자리다. <b>신분으로 분기하지 않는다</b>: 누구든 여섯을 거느리면 된다.
     */
    public static final int SCHOOL_MIN_FOLLOWERS = 6;

    /** 부지 탐색 — 세운 자의 집에서 이 거리부터 이 거리까지 고리를 넓히며 찾는다. */
    public static final int MIN_RADIUS = 12;
    public static final int SEARCH_RADIUS = 64;

    /**
     * 거처와의 여유 — 시설 반경 + 이웃 거처 반경에 이만큼 더 띄운다(사람이 지나갈 길).
     *
     * <p><b>집끼리의 규칙과 같은 2 다.</b> 처음에 4 를 쓴 것은 근거 없는 값이었고, 학교 반경이
     * 약 14 라 집마다 26블록 이상 떨어져야 했다 — 집 35채가 모인 마을 한복판에는 그런 구멍이
     * 없다. 실측(D19~21): 이용자 무게중심 반경 64 안에서 이틀 연속 "자리 없음", 사흘째에
     * 탐색 반경의 <b>맨 바깥</b>인 64블록에 세워졌고, 그 결과 소년 최근접거리 중앙이 49 로
     * 통학 한계 48 을 넘었다.
     *
     * <p>학교는 마을 <b>안에</b> 있어야 하는 건물이다. 겹치지 않게 하는 것은 반경의 합이
     * 이미 보장하므로, 그 위의 여유는 집끼리와 같은 눈금이면 족하다.
     */
    public static final int HOME_MARGIN = 2;

    /** 부지 지형 낙차 상한 — 이보다 울퉁불퉁하면 메움·파냄이 지형을 헤집는다. */
    public static final int MAX_SPREAD = 4;

    // ── 학교 운영(P5b) ─────────────────────────────────────────────────────
    /**
     * 하루 수업료 — 학생 가구 저장고에서 학교 주인에게.
     *
     * <p>"소액"(계획서 1.5)이다. 세금({@link com.evosim.core.Tribute#TAX_PER_FOLLOWER_DAY} 1.0)의
     * 절반이라, 아이를 학교에 보내는 것이 세금 한 번 더 내는 것보다는 가볍다. 못 내면 빚이
     * 되고 그 빚이 예속을 깊게 한다 — <b>시설은 예속의 도구다.</b>
     */
    public static final double TUITION_PER_DAY = 0.5;

    /**
     * 교사 하루 급여 — 학교 주인이 교사 가구에 낸다.
     *
     * <p>1.0 이면 학생 <b>셋</b>부터 흑자다(0.5×3 = 1.5 > 1.0). 계획서 1.5 의 "이용자가 많을수록
     * 이익이므로 세력이 클수록 남는 장사이고, 작은 세력은 적자라 시설을 못 짓는다"가
     * 이 두 수의 비로 성립한다. 착공 문턱이 추종자 6명인 것과 눈금이 맞는다.
     */
    public static final double TEACHER_WAGE_PER_DAY = 1.0;

    /**
     * 등교 하루당 <b>학교 주인에게 쌓이는 신세</b> — 수업료와 별개다.
     *
     * <p>왜 둘 다인가: 소액 수업료는 교육의 값을 다 치르지 못한다. 그 차액이 은혜로 남는 것이
     * 후원(patronage)의 실체다. 소작이 임금을 받으면서도 신세를 쌓는 것
     * ({@link AllegianceStore#W_TENANCY})과 같은 구조이고, 그 선례가 이미 이 코드에 있다.
     *
     * <p>1.0 은 감쇠 0.95 의 균형점이 20 이라, 100타일 지주의 추종 임계(20)를 <b>겨우</b> 넘는
     * 눈금이다. 큰 영주(200타일 → 임계 40)는 안 묶이고 중소 지주만 묶인다 — 이것이
     * <b>사슬을 붙잡는 못</b>이다(P4·P5a 실측: 지주 간 신세를 채워줄 경로가 없어 지배 계층이
     * 생겼다 사라졌다 했다).
     */
    public static final double W_SCHOOLING = 1.0;

    /**
     * 통학 한계(블록) — <b>실측으로 오르내린 끝의 48</b>.
     *
     * <p>처음에는 밭 통근({@code FarmTicker.COMMUTE})과 같은 48 을 썼다. 근거는 "성년의 통근
     * 한계가 이미 그 기준"이라는 것이었는데, 소년은 이동속도가 0.8 이라 같은 시간에 같은
     * 거리를 못 간다 — 어른의 눈금을 아이에게 그대로 쓴 것이 틀렸다.
     *
     * <p>거리별 도착 여부를 재니 경계가 드러났었다(D22): 착석한 아이의 최대 거리 31,
     * 도착 못 한 아이의 최소 거리 37. 그래서 한때 32 로 내렸다.
     *
     * <p><b>다시 48 로 올린다.</b> 32 로는 등록 자체가 0 이 되는 마을이 나왔다 — 21×21 건물이
     * 집 간격(15~19)보다 넓어 마을 <b>안</b>에 못 들어가고, 가장자리에 서면 32 안에 드는
     * 아이가 하나도 없었다(실측: 한계32 안 0/6명 · 0/9명). 등록이 0 이면 등하교도 능력치도
     * 관측 자체가 불가능하다.
     *
     * <p>대신 <b>등록과 실제착석을 나눠 찍는다</b>. 둘이 벌어지면 그 차이가 곧 "닿지 못한
     * 아이" 의 수이고, 수업료는 받고 능력은 안 올려주는 상태가 눈에 보인다 — 숨기지 않는다.
     *
     * <p><b>96 으로 올린다.</b> 48 을 정당화하던 근거가 측정으로 무너졌기 때문이다. 아이가
     * 멀리 못 간다고 본 것은 <b>활동반경 리시</b>(32)가 등교를 선점해 되끌던 것이었고, 그것을
     * 고친 뒤 47블록 착석이 나왔다(포기 0건). 길찾기는 56블록까지 닿고(navprobe), 시간도
     * 남는다 — 노동은 8000틱인데 96블록 등굣길은 0.95속도로 약 580틱, <b>7%</b>다.
     * 귀가는 노동이 끝난 뒤라 노동 시간을 쓰지 않는다.
     *
     * <p><b>왜 거리를 장벽으로 두지 않는가</b>: 계획서 1.5 의 장벽은 "추종 가구의 소년",
     * 곧 <b>예속 관계</b>다. 실측한 걸림돌은 가구전체안따름 24 · 통학초과 13 · 자리없음 0 이라,
     * 거리가 예속보다 먼저 걸러내며 봉건적 장벽을 지리적 우연으로 가리고 있었다. 거리를
     * 넓히면 <b>예속이 주된 장벽으로 드러나고</b>, 정원(자리 7)이 비로소 진짜 병목이 된다 —
     * 아홉 번의 스냅샷에서 {@code 자리없음} 은 내내 0 이었다. 그러면 둘째 학교가 "멀어서"가
     * 아니라 "꽉 차서" 서게 되는데, 그것이 {@code unservedStudents} 가 원래 노린 동작이다.
     */
    public static final double COMMUTE_RANGE = 96.0;

    /**
     * 이만큼의 학생이 어느 학교에도 안 닿으면 <b>한 채 더</b> 짓는다.
     *
     * <p>3 은 교사 급여의 손익분기와 같은 수다(수업료 0.5 × 3 = 1.5 > 급여 1.0). 새 학교가
     * 서자마자 흑자가 되는 자리에서만 지어지므로 수요 없이 늘어나지 않고, 마을이 바깥으로
     * 자라도 학교가 따라간다.
     */
    public static final int UNSERVED_TO_BUILD = 3;

    /**
     * 학교끼리의 최소 간격(블록).
     *
     * <p>겹치면 같은 아이를 두 학교가 나눠 가지며 둘 다 정원을 못 채운다 — 교사 급여는 각각
     * 나가므로 둘 다 적자가 된다. 실측이 그것을 보여줬다: 한 사람이 두 채를 지어 학생 3명·1명으로
     * 나눠 가지며 둘 다 적자였다(순 −58 · −61).
     *
     * <p>수요 판정(빈자리 있는 학교가 닿는가)만으로도 대개 막히지만, 그것은 <b>그날의</b>
     * 등록 수를 보므로 아이가 자라 빠지는 날에 구멍이 생긴다. 거리는 그런 흔들림과 무관한
     * 하한이다.
     *
     * <p><b>통학 한계의 두 배로 묶는 것을 그만둔다(192 → 96).</b> 통학 한계를 96 으로 올릴 때
     * 두 배 관계를 기계적으로 지켜 192 로 따라 올렸는데, 그것이 <b>정말 필요한 둘째 학교까지
     * 막았다</b>. 실측: 자리 7 이 꽉 차 5명이 돌아가는 날에도 87·91·109블록 지점의 착공이 세 번
     * 연속 "기존 학교와 N블록 (최소 192)" 로 보류됐다. 정원 부족과 확장 금지가 동시에 성립하는
     * 상태였다.
     *
     * <p>두 배 관계 자체가 근거가 약했다. 그것은 "두 통학권 원이 겹치지 않는 거리" 인데,
     * 겹침이 해로운 이유는 <b>정원을 못 채워 적자가 나는 것</b>이지 원이 겹치는 것 자체가
     * 아니다. 정원이 이미 찼다면 겹쳐도 둘 다 채워지므로 해가 없다. 그 판단은 수요 판정
     * ({@code FarmTicker.unservedStudents} — 빈자리 있는 학교가 닿으면 미충족으로 세지 않는다)이
     * 이미 하고 있으므로, 거리는 <b>난립만 막는 하한</b>으로 되돌린다.
     */
    public static final double SCHOOL_MIN_GAP = 96.0;

    // ── 교회(P6) ───────────────────────────────────────────────────────────
    /**
     * 교회 건축비 — 큰 것과 작은 것.
     *
     * <p>도면 부피로 눈금을 맞췄다: 학교가 1336칸에 60 이므로 블록당 0.045 이고, 교회는
     * 1177칸 → 53, 작은 교회는 459칸 → 21 이 나온다. 거기서 <b>50 / 20</b> 으로 정리한다.
     * 집 등급(중형 12 · 대형 30 · 저택 70)과 겹치지 않는 자리이기도 하다 — 겹치면 "저택을
     * 지을 수 있는 자만 교회를 지을 수 있는" 상태가 되어 두 선택지가 하나로 붙는다.
     */
    public static final double CHURCH_COST = 50.0;
    public static final double SMALL_CHURCH_COST = 20.0;

    /**
     * 교회를 세울 자격 — 거느린 자가 이만큼 있어야 한다.
     *
     * <p>학교({@link #SCHOOL_MIN_FOLLOWERS} 6)보다 <b>낮다</b>. 작은 교회는 건축비가 20 으로
     * 학교의 3분의 1이라, 같은 문턱을 두면 "지을 돈은 있는데 자격이 없는" 구간이 길어진다.
     * 계획서 1.5 의 "작은 세력은 적자라 시설을 못 짓는다" 는 <b>수지</b>로 성립해야지 자격
     * 문턱으로 두 번 막을 일이 아니다.
     */
    public static final int CHURCH_MIN_FOLLOWERS = 4;

    /**
     * <b>크기를 정하는 반경</b> — 이 안의 성년을 세어 큰 교회를 지을지 정한다.
     *
     * <p>학교와 달리 교회는 추종자 전용이 아니다(계획서 1.5: "확률적 방문"). 그래서 세는
     * 대상도 추종자가 아니라 <b>주변의 쓸 수 있는 사람 전부</b>다 — 그것이 이 교회가 실제로
     * 받을 수 있는 최대 인원이다.
     */
    public static final double CHURCH_REACH = 64.0;

    /**
     * 하루 방문 상한 — 큰 교회와 작은 교회의 <b>유일한 차이</b>.
     *
     * <p>효과(감쇠 완화·구애 우위)는 두 크기가 <b>같다</b>. 큰 교회가 방문자당 더 좋은 효과를
     * 주면 "품질" 로 이겨 작은 교회가 무의미해지고, 그러면 크기 선택이 사라진다. 차이를 용량에만
     * 두면 선택이 순수하게 경제적이 된다 — 계획서 1.5 그대로: 이용자가 많을수록 이익이므로
     * 세력이 클수록 남는 장사이고, 작은 세력이 큰 것을 지으면 자리가 비는데 급여는 다 나가
     * 적자다.
     *
     * <p>계획서 1.5 의 "하루 방문 상한으로 몰림 방지" 가 이 수다. 도면의 자리 수를 쓰지 않는
     * 이유: 작은 교회는 독서대가 1개뿐이라 상한이 하루 1명이 되어 버린다(큰 교회는 독서대가
     * 아예 없어 문간 안쪽이 자리로 대체된다). 방문은 앉는 것이 아니므로 자리 수와 무관하게
     * 따로 둔다.
     */
    public static final int SMALL_CHURCH_CAP = 4;
    public static final int CHURCH_CAP = 12;

    /**
     * 교회끼리의 최소 간격(블록) — 크기를 가리지 않는다.
     *
     * <p>학교와 같은 96 이다. 학교에서 이 값을 192 로 올렸다가 <b>정말 필요한 둘째 건물까지
     * 막는 것</b>을 실측했으므로({@link #SCHOOL_MIN_GAP} 주석), 처음부터 그 교훈을 적용한다.
     * 난립만 막고, 수요가 넘치는지는 방문 상한이 판단한다.
     */
    public static final double CHURCH_MIN_GAP = 96.0;

    /**
     * 방문 1회 헌금 — 방문자 가구 저장고에서 교회 주인에게.
     *
     * <p>계획서 1.5 의 "소액 헌금". 수업료({@link #TUITION_PER_DAY} 0.5)의 절반이다 — 등교는
     * 매일이지만 방문은 이따금이라 1회당 부담이 더 가벼울 이유가 없고, 오히려 <b>가난해도 갈 수
     * 있어야</b> 교회가 추종에 매이지 않는 시설로 성립한다. 못 내면 빚이 되어 예속이 깊어지는
     * 것은 학교와 같다.
     */
    public static final double TITHE_PER_VISIT = 0.25;

    /**
     * 성직자 하루 급여 — 교회 주인이 성직자 가구에 낸다.
     *
     * <p>0.5 면 하루 방문 <b>둘</b>부터 흑자다(0.25×2 = 0.5). 작은 교회 상한이 4 이므로 절반만
     * 차도 남고, 큰 교회(상한 12)는 훨씬 크게 남는다 — 계획서 1.5 의 "이용자가 많을수록
     * 이익이므로 세력이 클수록 남는 장사" 가 두 크기 사이에서 성립한다.
     */
    public static final double CLERGY_WAGE_PER_DAY = 0.5;

    /**
     * 방문 1회당 <b>교회 주인에게 쌓이는 신세</b> — 헌금과 별개다.
     *
     * <p>학교의 {@link #W_SCHOOLING} 과 같은 구조다: 소액 헌금이 위안의 값을 다 치르지 못하고
     * 그 차액이 은혜로 남는다. 다만 <b>1.5 로 학교(1.0)보다 크다</b> — 등교는 매일이지만 방문은
     * 이따금이라, 같은 값이면 교회로 생기는 사슬이 학교보다 훨씬 얇아진다. P6 의 목표가
     * 지주 간 사슬을 만드는 것이므로 1회당 무게를 키운다.
     */
    public static final double W_CHURCH = 1.5;

    // ── 막사(군인) ─────────────────────────────────────────────────────────
    /**
     * 막사 건축비 — 도면 부피로 눈금을 맞춘다.
     *
     * <p>학교가 1336칸에 60 이므로 블록당 0.045 다. 막사는 1768칸 중 공기·동굴공기 1044칸을
     * 뺀 <b>724칸</b>이 실자재라 32.6 → <b>32</b>. 작은교회(20)보다 크고 학교(60)보다 작다.
     */
    public static final double BARRACKS_COST = 32.0;

    /**
     * 막사 착공 자격 — 추종자 수. 교회(4)와 학교(6) 사이에 둔다.
     *
     * <p>아래 {@link #HOUSEHOLDS_PER_SOLDIER} 로 정원 1명이 나오는 최소 추종자가 4 이므로,
     * 그보다 낮으면 병사 0명짜리 막사가 선다.
     */
    public static final int BARRACKS_MIN_FOLLOWERS = 5;

    /**
     * 막사끼리의 최소 간격 — 학교·교회와 같은 96.
     *
     * <p>난립만 막는 하한이다. 경계 범위가 모자라 둘째가 필요한지는 아래 "못 지키는 가구 수"가
     * 판단한다(학교의 {@code unservedStudents} 와 같은 구조).
     */
    public static final double BARRACKS_MIN_GAP = 96.0;

    /**
     * <b>갈래가 다른 시설끼리</b>의 최소 간격.
     *
     * <p>종전에는 이 규칙이 <b>아예 없었다</b> — 학교·교회·막사가 서로 벽을 맞대도 아무 판정에
     * 걸리지 않아 마을이 답답해 보였다(육안 관측). 같은 갈래의 96 을 그대로 쓰면 안 된다:
     * 갈래가 다르면 서로의 수요를 대체하지 않으므로(교회가 학교 수요를 못 채운다) 정말 필요한
     * 건물까지 막힌다 — 학교에서 96→192 로 올렸다가 겪은 그 실측이다.
     *
     * <p>그래서 "벽이 붙는 것"만 막는 값으로 둔다. 건물 폭이 13~19 이므로 24 면 사이에 길과
     * 여백이 남는다.
     */
    public static final double FACILITY_CROSS_GAP = 24.0;

    /** 군인 1명이 지키는 추종 가구 수 — 정원 = min(막사 자리, 추종가구 ÷ 이 값). */
    public static final int HOUSEHOLDS_PER_SOLDIER = 4;

    /**
     * 군인 하루 봉급의 <b>최대</b>(가난한 병사) — 소작 임금 3.3(기회비용)과 성년 소모 3.0 을
     * 둘 다 넘어야 한다. 안 넘으면 아무도 군인이 안 되거나, 군인이 될수록 집안이 가난해진다.
     */
    public static final double SOLDIER_WAGE_MAX = 4.0;

    /**
     * 군인 하루 봉급의 <b>바닥</b>(부유한 병사) — 교사 급여와 같은 눈금.
     *
     * <p>이 바닥이 균형을 만든다: 정원 산출(≈5.0) + 1.0 = 2인 가구 소모(6.0). 군인 가구도
     * 소작처럼 기준선 근처에서 고정된다. 바닥이 0 이면 부유해진 병사는 무급이 되어 이탈하고
     * 가난해지면 복귀하는 진동이 생긴다.
     */
    public static final double SOLDIER_WAGE_MIN = 1.0;

    /** 봉급 감쇠 기준선 = 병사 가구 <b>성인</b> 명목소모 × 이 일수(누진 지대와 같은 눈금). */
    public static final double SOLDIER_WAGE_CAP_DAYS = 3.5;

    /**
     * <b>막사 착공에 요구하는 봉급 운영자금</b> — 정원 × {@link #SOLDIER_WAGE_MAX} × 이 일수.
     *
     * <p>종전 문턱은 건축비 + 과시여유(=44)뿐이었다. 그것은 <b>현금 한 번</b>을 보는 검사라,
     * 매일 나갈 봉급을 전혀 보지 않는다. 그래서 24타일짜리 밭을 가진 지주가 막사를 세우고
     * 곧바로 쪼들렸다(실측: 밭이 자금 부족으로 24에서 멈춘 채 막사가 섬). 상비군은 한 번
     * 치르는 값이 아니라 <b>매일 나가는 값</b>이므로 문턱도 그 형태여야 한다.
     *
     * <p>새 문턱 = 건축비 + 과시여유 + 정원 × 최대봉급 × 5일. 값을 새로 지어내지 않고
     * <b>실제 청구서에서 그대로 끌어온다</b> — 정원은 이미 지킬 가구 수에서 나오고(4가구당
     * 1명), 최대봉급은 가난한 병사에게 나가는 그 수다. 그래서 세력이 커질수록 문턱도 같이
     * 커진다: 추종 5가구(정원 1)면 44 → 64, 추종 20가구(정원 5)면 44 → 144.
     *
     * <p>5일인 이유 — 봉급 감쇠 기준선({@link #SOLDIER_WAGE_CAP_DAYS} 3.5)보다 길게 잡는다.
     * 병사 저장고가 기준선에 닿으면 봉급이 최소치로 내려가므로, 최대 봉급이 실제로 나가는
     * 구간은 그보다 짧다. 즉 5일치를 쥐고 시작하면 최악의 구간을 넘길 수 있다.
     */
    public static final double BARRACKS_RUNWAY_DAYS = 5.0;

    /** 봉급 미납이 이 일수 연속되면 이탈 — 부양력이 <b>실제 상한</b>이 되게 하는 장치. */
    public static final int SOLDIER_DESERT_DAYS = 2;

    /**
     * 보호세 — 추종 가구가 하루에 내는 <b>저장고 비율</b>. 캡은 아래.
     *
     * <p>정액이 아니라 비율인 것은 가난한 가구를 사실상 면세로 두기 위해서다(저장고 5 면
     * 0.05). {@code Tribute.payable} 이 2일치를 남기므로 위태로운 가구는 아예 0 이다.
     */
    public static final double GUARD_TAX_RATE = 0.01;

    /**
     * 보호세 가구당 하루 <b>상한</b>.
     *
     * <p>이 캡이 "부담 감소이지 수익원은 아니다"를 보장한다. 캡이 없으면 부유한 추종자에게서
     * 세수가 폭증해 고참 봉급(바닥 1.0)을 넘어 <b>지주가 군대로 흑자</b>를 낸다.
     * 군인 1명이 4가구를 지키므로 군인당 세수는 최대 1.2, 평균 0.8 — 어떤 경우에도 지주가
     * 자기 몫을 낸다.
     */
    public static final double GUARD_TAX_CAP = 0.3;

    // ── 세력 통합(WAR-PLAN.md) ────────────────────────────────────────────
    /**
     * <b>압박 신세</b> — 병사가 그날 독자세력 머리의 집에 닿으면 그가 지는 신세(탕감분).
     *
     * <p>1.5 는 임의 수가 아니다. 굴복 문턱은 {@code max(MIN_BOND 4, 타일 × TILE_WORTH 0.2)}
     * 이고, 적립 체감(ACCRUE_FREE 12 ~ CAP 60)에서 W=1.5 의 균형점이 <b>23.1</b> 이다:
     * <pre>
     *   30타일(문턱 6)  → 약 4일에 굴복
     *   60타일(문턱 12) → 약 10일에 굴복
     *   115타일(문턱 23) → 경계선
     *   200타일(문턱 40) → <b>압박만으로는 영영 굴복 안 함</b>
     * </pre>
     * 마지막 줄이 설계의 핵심이다 — 작은 독립세력은 협박으로 접히고, <b>대등한 상대는
     * 전쟁으로만 갈린다</b>. 두 경로를 나눈 근거가 이 산술이다.
     *
     * <p>빚(상환분)이 아니라 탕감분인 이유: 탕감분은 감쇠한다. 막사가 무너지거나 병사가
     * 이탈해 압박이 끊기면 굴복이 풀린다. 빚은 이자로 불어나 영구화되어 반란이 구조적으로
     * 불가능해진다.
     */
    public static final double W_PRESSURE = 1.5;

    /** 전투 가능 병사 0 이 이만큼 연속되어야 막사가 넘어간다 — 하루 운으로 주권이 바뀌지
     *  않게, 그리고 증원이 도착할 시간을 벌기 위해. */
    public static final int OCCUPY_DAYS = 2;

    /** 교전 막사로 파견할 때 각 막사가 남겨둘 최소 수비. 초과분만 보낸다. */
    public static final int GARRISON_MIN = 2;

    /**
     * <b>파견 반경</b> — 교전 막사로 불려올 수 있는 최대 거리(평시 배속은 {@link #COMMUTE_RANGE}).
     *
     * <p>160 은 미믹의 {@code FOLLOW_RANGE} 와 같은 값이다. 그 속성이 경로 탐색의 상한이라,
     * 이보다 먼 목적지는 리시가 <b>경로를 만들지 못해 그 자리에 얼어붙는다</b>(같은 함정을
     * 구걸에서 이미 겪었고, 거기서는 96블록 경유지를 놓아 223블록을 완주했다).
     *
     * <p>먼저 이 값으로 재고, 파견 병사가 실제로 도착하지 못하면 그때 경유지 장치를 붙인다 —
     * 미리 붙여 놓고 "될 것이다"라고 하지 않는다.
     */
    public static final double DISPATCH_RANGE = 160.0;

    /**
     * <b>후송 급양</b> — 부상병이 아군 막사에 닿으면 영주 저장고에서 병사 H 로 넘어가는 양.
     *
     * <p>회복 자체는 새로 만들지 않는다. {@link MimicEntity} 의 재생이 이미
     * {@code holding > 0} 에 물려 있어, 급양 경로만 이으면 <b>지배자의 지갑이 곧
     * 전투지속력</b>이 된다: 적의 저장고가 마르면 급양이 끊기고 → 회복이 멎고 →
     * 부상병이 계속 전투불가로 남고 → {@link #OCCUPY_DAYS} 뒤 막사가 넘어간다.
     *
     * <p>회복은 40틱마다 0.5 HP 라 30%→70% 가 약 640틱이다. <b>시간은 병목이 아니고
     * 급양이 병목이다.</b> 병사 12명이 하루 한 번씩 후송되면 하루 24 — 관측된 영주
     * 저장고가 60~130 이니 부자는 버티고 빈자는 며칠 만에 마른다.
     */
    public static final double MEDIC_RATION = 2.0;

    /**
     * <b>전사자 유족 보상</b> — 가구 성인 명목 하루소모 × 이 일수를, 영주 저장고에서 지급.
     *
     * <p>군인 선발 조건에 {@code isProviderRole()} 이 있어 <b>모든 군인은 가구 부양자</b>다.
     * 전사 1명은 가구 1개의 붕괴다 — 그것은 전쟁의 대가이지 결함이 아니므로, 이 보상은
     * 완충이지 면제가 아니다. 7일 = 다른 부양자를 찾을 시간.
     *
     * <p>구휼({@code W_RELIEF})로 기록한다 — 유족이 영주에게 신세를 지므로 <b>전쟁 손실이
     * 오히려 가문을 결속시킨다</b>.
     */
    public static final double DEATH_BENEFIT_DAYS = 7.0;

}
